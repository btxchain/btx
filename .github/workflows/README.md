# GitHub Actions disabled

This repository does not use GitHub-hosted CI for validation.

Workflow YAML files are intentionally absent from `.github/workflows` so pull
requests and pushes do not trigger GitHub Actions checks. Use the repo-local
build and test commands documented in the PR or release instructions instead.
