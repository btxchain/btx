# Shell/python scripts MUST stay LF: CRLF makes bash 'case run\r)' never match and
# scripts die silently inside WSL. PowerShell stays native.
*.sh text eol=lf
*.py text eol=lf
*.pyw text eol=lf
*.ps1 text eol=crlf
