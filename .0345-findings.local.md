# 0.34.5 crash dump (2026-08-28)

Untracked-style dump so a context OOM does not lose the day’s measurements.
Push this file. Do not treat GPU-node height as the seed-discovery question.

## Settled ancestry (do not re-litigate)

- Walked live from headers-only 199384 `2e6394bc0ad6a055…`:
  - ancestor@199295 is `33c834f8…` **True**
  - ancestor@199294 is active `0ca79e60…` **True**
- Live chain descends from the block Case A told operators to `invalidateblock`.
- That instruction is retracted (PR 125 + v0.34.3/v0.34.4 notes). Recover: `reconsiderblock 33c834f8056aca85a8591a304fe52affebe2770d6027e79845765547f8dfae82`.
- Seeds are `-matmulvalidation=relay` (ADDR-only, NODE_NETWORK stripped). Height 199294 on a seed is **irrelevant**. Do not make them converge.

## Seed discovery (highest-value measurement today)

Compiled `chainparamsseeds.h` (BIP155): `146.190.179.86:19335` and `164.90.246.229:19335`.
DNS: `node.btx.dev`, `node.btx.tools`, `node.btxchain.org`.
Both compiled IPs are the two relays. Third DNS name is 0.34.0 at 199300.

Live RPC on both relays (2026-08-28 ~08:00Z):

- `discovery_relay=true`, `discovery_archive_reported_height=-1` on **both**.
- `matmulvalidation=relay`, `/BTX:0.34.4/`, `blocks=headers=199294`.
- `-connect=` only to sibling relay + the 0.34.0 DNS host. **Two outbound each.**
- Those two outbounds advertise services `WITNESS, SHIELDED, P2P_V2, MATMUL_DISCOVERY` — **no NODE_NETWORK, no CONSENSUS, no ARCHIVE**. They cannot raise the GETADDR watermark (`MayRaiseArchiveReportedHeight` requires NETWORK + CONSENSUS|ARCHIVE|MIRROR). Watermark stays −1 ⇒ `PeerLooksOnRecentNetwork(*, -1)` is **vacuous** (every versioned peer passes).
- Connected: ~169 / ~157 (97–99% inbound).
  - tip-class ≥199000: 65 / 65
  - 185109-class: 59 / 58
  - below-185k: 38 / 27
- `getnodeaddresses 0`: 1081 / 1068 entries. Age p50 ~12d, p90 ~17d, max ~29d (IsTerrible horizon). NODE_NETWORK bit on ~70%. `servicesnames` empty on the RPC dump.

**Addrman vs connected (seed A, the diagnostic table):**

| cohort | connected AND in addrman | connected NOT in addrman |
|---|---|---|
| tip-class ≥199000 | **20** | **45** |
| 185109-class | **23** | **36** |
| below-185k | **12** | **26** |
| other | 2 | 4 |

- addrman overlap with connected: 50 of ~133 unique connected IPs.
- Of the 50 that addrman *does* remember while connected: 20 tip vs 23+12 stale ≈ **two-thirds stale**.
- 45 current peers the seed is actually connected to are **not** in addrman (inbound ADDR is not learned: `MayLearnAddressFromPeer` false for inbound).
- GETADDR serves addrman (height-blind) then extra-pushes only outbound/manual/addr_fetch peers. With 2 discovery-only outbounds, GETADDR is the stale addrman.

Signer GPU `getpeerinfo` same window: n=64, in=50, out=14. Buckets: 185109-class 11, tip-class 39, below-185k 14. **Outbound 8/14 are 185109-class.** startingheight_max 199854, 19 peers above 199312, synH_max 199384. Subver mix includes 0.32.12 and 0.33.x. (Operator “60 of 64 near 185109” was a different snapshot; live now is 25/64 stale-or-old, but **outbound is majority-stale**.)

**Relay aging verdict:** addrman has no height field. `IsTerrible` is time/attempt only. A reachable 185109 node that still answers TCP is refreshed forever. Relays keep serving far-behind addresses indefinitely. Relays do **not** need a chain for GETADDR (`IsDiscoveryRelay` early-returns block/header serving). Syncing to 199294 is wasted work and is **not** the stale-peer bias; the bias is inbound-not-learned + height-blind addrman + watermark stuck at −1.

## 72-block unauthenticated lead cap vs reconsiderblock split

- Unauth header lead 72 ⇒ headers-only sat at tip+72. Signer tip 199312 + 72 = **199384**, which matches synH_max. That explains the *header* stall shape, not why `reconsiderblock 33c834f8` moved one GPU and not the other.
- Second GPU: operator reports `reconsiderblock` 199310 → 199317 immediately. That is **+7 on whatever chain it was already on**. Park depth 6 would still refuse an 18-deep reorg back to LCA 199294. So +7 is **not** proof of rejoin onto 33c834f8; it may be island continuation. Check `getblockhash 199295` on that node (must be 33c834f8, not `8b5da5a5`).
- Signer GPU: still 199312 after the same RPC. Connecting 33c834f8 from 199312 is a depth-18 rewrite. `ed52178e` correctly does **not** pass stale-heavier as `recovery_escape`. Park should refuse auto-follow. That is a genuine protection, not a bug.
- 72-cap is **not** the two-GPU reconsiderblock split.

## Four download gates: genuine vs self-inflicted

| item | commits | verdict |
|---|---|---|
| must_probe scope | `8b0b0425` | **GENUINE. KEEP.** BestKnown height-ahead on a fork that does not extend the tip skipped getheaders. Bites any node. Has tests. |
| stranded `m_best_header` | 0.34.4 `EnsureBestHeaderNotBehindConnectedTip` | **GENUINE. KEEP.** headers-below-blocks. Bites any node. |
| heavier-fork hole keep / GETDATA | `bb96b230`, `77283c72` | **KEEP the fetch.** A lighter active tip vs a heavier valid sibling is a real case without any invalidate. Evaluate ≠ follow. |
| park escape for stale heavier | unwired in `ed52178e` | **KEEP unwired.** Auto-follow past park_depth=6 is the dump-and-run hole. |
| uncommitted island-skip / reserved RC lane / retained-body GETDATA fallthrough | dirty tree, not pushed | **DROP unless bootstrap proves otherwise.** These exist to drag bodies of a chain we had marked invalid. After reconsiderblock, either park refuses (correct) or the hole is just IBD. Do not ship complexity to compensate for our invalidate. |

## Security review (park / DoS / 51%)

- `DeepReorgShouldPark(PARK, 6, 8/90/151, recovery_escape=false)` still tested. `ed52178e` does not OR stale-heavier into `recovery_escape`.
- 90-header 199384 fork is a depth-18 disconnect from 199312; park **must** refuse auto-follow.
- 51% attacker still needs header PoW, ExactReplay, and valid txs to ConnectTip. GETDATA of a heavier fork is not follow.
- G2 `drop_bodies_before_deserialize=false` (body is self-validating). TX/MEMPOOL still dropped in authority/relay as designed.
- Watermark −1 makes the *introduction* height filter vacuous on seeds. That is a **discovery** hole, not a fork-choice hole. Does not let a peer force unbounded ExactReplay (GPU slot / competing-lane cap / HEADER_ONLY twins still exist).
- Uncommitted RC reserved-lane for the selected hole would let a chosen competing body take the followed lane — do not ship that unless a non-invalidate threat model needs it.

## Bootstrap test status (in progress after this dump)

- Unsealed `build-gcc13/btxd` **refused** to start: `build_provenance_mismatch` / no qualified ExactReplay (`RefuseUnverifiableMatMulConsensusStartup`). That is fail-closed, not a discovery result.
- Sealed v0.34.4 linux-x86_64-cpu is extracted at `.seed-bootstrap-opt.local/btx-0.34.4/`. Next: fresh datadir, `dnsseed=0`, compiled fixed seeds only, sealed `btxd`. Success in ~10 min = outbound not majority 185109-class, headers moving, GETADDR from the two compiled IPs.
- MendeMatthias parking at 191690 from a clean datadir is consistent with a majority-stale GETADDR set (bimodal: 185109 fleet + a thin 191k band), not with a 199294 seed tip.

## What not to do

- Do not `invalidateblock 33c834f8`. Do not make relay seeds sync the chain.
- Do not ship the dirty `net_processing.cpp` island/RC patches as 0.34.5 until bootstrap proves they are needed without invalidation.
- Do not treat seed height 199294 as stuck.
