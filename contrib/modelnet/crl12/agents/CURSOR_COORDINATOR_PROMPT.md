# Cursor coordinator: BTX Cognitive Reserve Layer v1.2

Execute after the current HCP/1 + Cognitive Reserve v1.1 work has fully handed off. Both earlier specs and the current tree are available. Continue that implementation; do not reset it or replace it with this package's reference code.

## Objective

Deliver a neutral, composable Cognitive Reserve Layer and institutional whole-portfolio interface in BTX 0.34.8. Any incumbent, specialist or new entrant must be able to implement the same roles. Corporate, institutional, family-office, asset-manager and portfolio-technology clients must be able to track actual financial reserves, commitments and productive capability without inventing financial rights or asset totals.

## Read before editing

Read START_HERE, agents/AGENTS.md, the full normative development spec, all operation/type schemas, the institutional guide, integration/UX guides and the entire acceptance catalogue. Read prior HCP/v1.1 handoffs and preserve unresolved prerequisites. The external strategy is framing, not authorization to alter money or omit security.

Audit the actual registered HCP routes, codecs, engine, journals, native funding, role/identity adapters, local connector, package verifier, capability APIs and UI. Record baseline fingerprints, active processes and production exclusions. Produce a REUSE/EXTEND/NEW contract map.

## Freeze the additive contract

Preserve all 84 preceding contract operations and old signed types. Add the 43 operations and 18 new V1_2 signed types in this package. The v1.2 discovery route is separate; old discovery remains unchanged. Reconcile any private naming conflict once before workers edit shared modules. Do not introduce Core v4, another engine, another money ledger or another downloader.

Freeze provider-role/effect validation, service binding authority, namespaced asset identity, bitemporal observation selection, metric eligibility, duplicate and look-through rules, staged interchange, draft-only instruction translation and job status. All quantities remain exact, and unavailable valuations remain unavailable.

## Dispatch Grok 4.6 Extra High workers

Use the actual supported worker mechanism and confirm the exact model. Do not fabricate flags or silently substitute. Run four to six workers concurrently when resources permit. Give each an exclusive file set and frozen contract references. Coordinator owns shared headers, route/schema registry, CMake and one controlled integration build.

A: neutral roles, provider bindings, negotiation, source/config neutrality checks.
B: asset/rights records, source authority, bitemporal positions and durable ingestion.
C: valuation, metric policies, direct/look-through graphs and institutional projections.
D: import/export staging, mappings, jobs, reconciliation and restart/failover.
E: institutional adapters, draft translation to existing v1.1 plans, deterministic scenarios.
F: SDKs, neutral portal, desktop context, accessible workflows and local handoff.
G: independent native/process/chain/browser tests; can start against frozen contracts and fixtures.

## Implement end to end

A generic provider must join without code changes. A customer must be able to split discovery, custody and analytics among independently enrolled providers. A portfolio system must ingest exact records, show correct financial/operational views and prepare a normal v1.1 capital draft. Existing authority must still govern the actual finance and local capability effects.

Never turn institutional import into a custody credit. Never treat a model signature as rights/valuation authority. Never infer AUM from technology coverage or deployed copies. Never auto-replay unknown finance at another provider. A remote portfolio instruction must not execute money or inference.

Every listed route must reach a real registered production implementation. An operation ID or mock response does not satisfy the requirement. Preserve all prior 0.34.8 functionality and scope boundaries.

## Verify

Run scripts/validate_package.py and reference tests as package checks only. Implement the 160 new native cases and 20 whole-system journeys in the integrated tree. Preserve prior HCP/v1.1 and all existing regressions. Require real ML-DSA checks, authenticated multi-tenant requests, transactional multi-process replay, actual walletless local acquisition, isolated native funding/refund and real browser accessibility tests where specified.

Run the 100,000-row native process workload, and separately the declared ten-million-row institutional lab if available. Report actual payload/row counts, memory, latency, coverage and cleanup. Missing environments stay NOT_RUN. Do not use synthetic benchmark assumptions as achieved performance.

Fresh reviewers audit authority, asset/metric correctness, temporal recovery, privacy, neutral portability and actual call-path evidence. Fix real findings and rerun affected tests.

## Handoff

Return initial/final candidate fingerprints, preserved-contract comparison, exact routes/types, code map, migration/rollback evidence, all test results by tier, role-conformance manifests, local/financial outcomes, unresolved failures and next operator actions. Do not publish, push, merge, tag, flip release flags, replace production binaries, restart production or move live funds.
