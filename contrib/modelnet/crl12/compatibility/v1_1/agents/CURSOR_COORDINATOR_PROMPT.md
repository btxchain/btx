# Cursor coordinator — Cognitive Reserve v1.1

Execute this after the current HCP/1 worker round reaches a clean coordinated handoff. The existing conversation, original v1.0 package and current private tree are available. Continue that work; do not reset or rebuild a parallel product.

## Objective

Deliver the complete BTX0.34.8 Cognitive Reserve extension: corporate, institutional and family-office portfolios; BTX reserve policies; workload ownership comparisons; capital-allocation DAGs; committee approval; research programmes; holdings; existing-product integration; reports; seamless portal/SDK/device flow. Financial reserve, cognitive assets and native commitments remain distinct.

## Required sequence

Read START_HERE, all four documents, compatibility artifacts, schema/operation catalogue and the entire acceptance catalogue. Record baseline HEAD, dirty diff and relevant untracked hashes, current binaries/builds, production PIDs and outstanding base HCP/JIT tests. Map every requirement to existing code, a needed extension or an explicit gap.

Freeze exact new object names, body-ID domain, entity keys, capacity formula, parent/child operation identity and approval bindings. Preserve base HCP/1 bodies and 34 operations. Do not create Core v4, another HcpEngine, a second customer ledger or a new downloader.

## Parallel worker model and ownership

Use Grok 4.6 Extra High workers through the actual available Cursor task mechanism. Confirm the exact selection; do not invent flags or silently substitute. Dispatch four to six active workers subject to host resources. Assign exclusive files or isolated worktrees. Coordinator owns shared contracts, registrars, CMake, schema aggregation and final integration. Do not run simultaneous full builds in the same directory.

A — contracts, compatibility and SDK typing.
B — entity/portfolio/reserve policy and atomic capacity.
C — workload/TCO/capital DAG and child intent binding.
D — distinct-person approvals and co-sponsor programmes.
E — holdings, products, reports and exports.
F — portal, local connector, accessibility and browser journeys.
G — native process/chain/concurrency conformance; can follow shared freeze.

## Non-negotiable behavior

No change to native monetary consensus, ownership, issuance or frozen funding terms. No arbitrary public wallet/runtime proxy. Public models stay free of a compulsory exchange toll. No per-token inference business is added. CEX financial authorization is not a LocalCapabilityGrant. Reported assets, customer liabilities, cognitive holdings and commitments never merge into invented NAV.

A family/group view confers no debit authority. Human quorum counts distinct verified people, not sessions or agent keys. Signed or broadcast UNKNOWN outcomes keep their holds. A cross-CEX retry must not create a second financial action. Conversion followed by failed funding leaves BTX in the account. Refunds do not replenish lifetime mandate by default.

Use local evidence, not claims, for quality and readiness. An acquired recipe can remain useful after gateway failure. Do not ship exchange tokens into runtime environments. Do not release GPU buffers before physical completion because a portal job was canceled.

## Tests and completion

Implement every new case in tests/ACCEPTANCE_TESTS.md and every journey. Preserve and rerun the 120 base HCP cases plus relevant JIT/AHP regressions. Reference simulator success is not native evidence. Test exact OpenAPI base-operation preservation, canonical body vectors and real signature checks. Run multiple replicas against actual transactional persistence and inject crash/ambiguity at signer, broadcast and database boundaries.

Build the actual gateway, local client, SDKs and portal. Run actual native-chain/custody cases in isolated authorized environments. Verify walletless local acquisition, resident-base/LAN reuse, committees, reserve-floor concurrency, refunds and provider exit. Record unsupported environments as NOT_RUN without hiding unfinished production paths.

Fresh reviewers independently audit financial authority, ledger accounting, private data, compatibility, UI claims and native/runtime evidence. Fix real findings and rerun affected journeys.

## Stop boundary

Do not push, merge, tag, publish, flip IS_RELEASE, restart production, replace active binaries, move live funds or alter production custody. Use isolated test accounts, keys and prefixes. Return one final report with fingerprints, code map, route/type counts, tests by evidence tier, all FAIL/NOT_RUN, migration/recovery results and exact operator actions remaining.
