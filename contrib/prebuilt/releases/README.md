Bundled precompiled BTX CLI archives for this clean snapshot live here.

Files currently included:

- `btx-29.4.0-generic-win64-x86_64-w64-mingw32.zip`
- `btx-29.4.0-generic-win64-x86_64-w64-mingw32.zip.sha256`
- `btx-29.4.0-native-cli1-arm64-apple-darwin.tar.gz`
- `SHA256SUMS.native-cli1`
- `btx-release-manifest.native-cli1.json`

Notes:

- The Windows archive is the new generic `clang-cl` + Ninja x64 build from the
  public BTX tree, without machine-specific mining tuning.
- The macOS arm64 archive is the current upstream native CLI preview asset that
  was already published for BTX `v29.4.0-native-cli1`.
