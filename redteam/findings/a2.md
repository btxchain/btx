# a2 — MTP timestamp-ratchet / timewarp-forward / timelock maturation

Patched ruleon@200 image + "evil miner" variant (UpdateTime always emits nTime=cap=prev_MTP+3600), -mocktime. Cleaned up.

## RESULTS (empirical)
1. **Per-block ratchet BOUNDED — slow-motion, not runaway.** Mining at the cap every block: 300 blocks → tip & MTP each advanced EXACTLY 600 s/block. Mechanism: cap=prev_MTP+3600 but MTP is the median (6th of 11); a cap-stamped ts only becomes the median after ~6 blocks ⇒ effective MTP advance = 3600/6 = 600 s/block. Every block hits its cap (time==cap) but the median dampens.
2. **2h node-clock check (time-too-new) independently caps absolute lead.** From tip≈real-clock R: round 1 tip jumps to R+3603; sustained tip/MTP asymptote to R+7200 (MAX_FUTURE_BLOCK_TIME); next cap (R+7203) REJECTED "time-too-new". Verified the 2h rejection fires. ⇒ chain time can lead real wall-clock by AT MOST ~7200s (2h) — same bound stock Bitcoin already enforces. The +3600 drift rule is the tighter PER-STEP constraint but enables NO runaway.
3. **Timelock (BIP113) — NOT exploitable.** Inflated MTP feeds BIP113 locktime cutoff (validation.cpp:2317) but max MTP-ahead-of-real = ~7200s (2h), ceiling-limited by #2. So time-locked nLockTime/CSV can mature at most ~2h early — identical to pre-existing Bitcoin 2h tolerance; the new rule does NOT widen it. External proposal vector #4 (timelock maturation) = REFUTED.
4. **Difficulty (ASERT) suppression — not directly measurable** (this variant had fPowNoRetargeting=true, diff flat). Analytically the +3600 cap CONSTRAINS per-block ts jumps ⇒ constrains ASERT manipulation; any easing bounded by the same 2h one-time lead. (b4 measured the per-block ×1.97 ease directly.)

## VERDICT
MTP ratchet = bounded slow-motion (≤600 s/block), hard-capped ≤2h ahead of honest real wall-clock by MAX_FUTURE_BLOCK_TIME, which binds before any runaway timewarp. No timelock/difficulty consequence exploitable for profit. External vectors #1 (runaway ratchet) and #4 (timelock) REFUTED.

## Refuted / robust (don't repeat)
- MTP-ratchet is NOT unbounded (median dampens to 600s/block; 2h clock cap binds). Timelock maturation bounded to pre-existing 2h tolerance.
