# a3 — MatMul validation & relay hardening (payload/header DoS)

Isolated regtest, `rtA3-` namespace, `btx-node:pr214` + patched `rtA3-eps18` (epsilon=18) under `-test=matmulstrict`. Cleaned up; repo tree unmodified.

## Vectors attempted
- **C1/C2 product-payload validation** — Python bit-exact block builder; submitted variants to fresh first-seen node:
  - clean canonical C' → ACCEPTED; corrupted word → `invalid-product-payload` (BLOCK_MUTATED, validation.cpp:8577 "non-canonical"); value ≥ MODULUS → `invalid-product-payload`; over/undersized → `invalid-product-payload` ("malformed" :8572); empty → `missing-product-payload` (:8568).
  RESULT: guards HOLD. **Consensus tightening CONFIRMED (informational/low):** when product-digest active, a size-valid C'-payload block that fails Freivalds/product-committed is rejected at :8577 BEFORE the O(n^3) Phase2 fallback (:8584). On main such a block (if matmul_digest is a *transcript* digest) is ACCEPTED. So: a transcript-digest block ALSO carrying a C' payload = accepted by main, rejected by PR-214 at digest-active heights (regtest 0; mainnet 61000).
- **C3 header pre-hash 'high-hash' gate** (eps18 variant, matmulstrict): honest mining still works (no orphaning). KEY: regtest work target << 18 bits SATURATES (SaturatingLeftShift256, pow.cpp:588) ⇒ pre-hash target = 2^256−1, every sigma passes; the gate is a STRICT RELAXATION of the work check ⇒ any header meeting real work target satisfies the looser pre-hash filter ⇒ **cannot false-positive-orphan by construction** (mainnet too). DeriveSigma is O(1) (2×SHA256) before expensive matmul ⇒ no work amplification. Fail ⇒ high-hash ⇒ 100 ban one-shot.
- **C1 BLOCK_MUTATED griefing / re-download loops:** block hash is header-only; matrix_c uncommitted. BLOCK_MUTATED NOT marked BLOCK_FAILED_VALID (re-validatable by design). Verified: 10 bad-payload rejects do NOT poison the real block (clean block same hash still ACCEPTED). Cost ~125ms/validation but **only via local authenticated submitblock RPC**; over P2P a non-compact BLOCK_MUTATED → HandleDoSPunishment(...,100) → ban after ONE attempt (net_processing.cpp:2251). No remote loop.
- **C4 submitblock guard:** odd-hex → pre-decode reject; 48MB body → HTTP 413 (evhttp max body 32MB) before handler, mem flat; 31MB garbage → "Block decode failed" 2.25s mem flat; compactsize bomb (declares 33.5M words ≈134MB) → decode-fail, mem flat (MAX_VECTOR_ALLOCATE chunking caps speculative alloc ~5MB). No bypass/crash.

## NEW / findings
- **Product-digest tightening orphan risk = LOW** | low/informational | repro: post-activation block with transcript-digest + C' payload → rejected by PR-214, accepted by main. | MITIGATION: none needed in code IF all miners conformant (reference miner only emits product-committed-digest blocks, pow.cpp:2791; the two digest schemes never collide). ACTION: confirm no third-party/legacy miners emit transcript-digest+payload blocks at mainnet heights ≥61000 before relying on it. The O(n^3) fallback at :8584 is now dead code for payload-carrying digest-active blocks.

## Refuted / robust (don't repeat)
- BLOCK_MUTATED has NO remote DoS amplification (P2P one-shot ban); it's a DoS-resistance IMPROVEMENT (bad payload can't permanently invalidate the real block).
- submitblock guards (hex parity, size, compactsize) all hold; no crash/bypass.
- Header pre-hash gate cannot orphan honest blocks (strict relaxation of work check); no work amplification.
