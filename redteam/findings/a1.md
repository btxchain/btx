# a1 — ASERT difficulty grind within the cap + liveness

Self-built correct rule-active image (rtA1-ruleon, height=200) — flagged shared ruleon image is rule-DISABLED. ASERT live via `-test=matmuldgw` (regtest HL=14400, anchor=genesis). EVIL=every block stamped at ceiling prev_mtp+3600 (via setmocktime advancing each block); HONEST=+90s/block.

## RESULTS
- **V1 difficulty suppression (a1 rated HIGH):** capped-evil 100 blk → difficulty collapsed to powLimit floor (−59% from elevated start). HONEST 100 blk → 0% drift.
- **V2 MTP ratchet:** MTP advanced ~584 s/block (6.5× target) — CONFIRMS a2's 600s/block. Chain clock +16h over 100 blk. Genesis-anchored ASERT reads sustained 6.5× spacing as ~84% hashrate shortfall → floor; a1 reports difficulty stayed suppressed after attack stopped.
- **Liveness PASS:** simulated +3h/+24h real stalls → honest miner still produces valid blocks (UpdateTime clamps nTime into the past to prev_mtp+3600, curtime_clamped=true), self-heals. Rule does NOT cause a stuck chain.
- Enforcement: consensus check unconditional for height≥200; miner refuses to exceed cap even at mocktime=2e9. No single over-cap block landed.

## ★ ORCHESTRATOR RECONCILIATION (a1 vs a2 — threat-model artifact)
a1's "collapse to floor" used setmocktime advancing the validator clock to FOLLOW the chain each block ⇒ the MAX_FUTURE_BLOCK_TIME (2h, "time-too-new") check NEVER fired ⇒ UNBOUNDED fake-elapsed-time injection. a2 kept the validator clock at REAL time ⇒ the 2h check binds at tip=R+7200 ⇒ ratchet pinned. Against HONEST nodes with real clocks (realistic network): chain time ≤ R+7200, so max injectable fake elapsed time ≈ the 2h buffer (one-time), giving max difficulty drop ≈ 2^(7200/halflife): **mainnet HL=3600 ⇒ ~×4 (to 1/4), transient, self-correcting** as honest fast blocks close the schedule gap — NOT to the floor. a1's floor result is the free-mocktime artifact (a1 acknowledges the caveat).
- SEVERITY (realistic) = MEDIUM, not HIGH: bounded ~×4 transient difficulty suppression + per-block ~×2 lever; self-corrects. Genesis-anchor makes the injected gap "sticky" until honest blocks at the lowered difficulty work it off (fast, since low diff ⇒ fast blocks).
- GAP: neither a1 (free mocktime) nor a2 (fPowNoRetargeting=true, diff flat) measured DIFFICULTY under a REALISTIC advancing-real-clock + live-ASERT + mainnet-HL. → tie-breaker b7.

## Fix implication
Confirms fix #2 (tighten per-block drift below one half-life) is the right lever — it directly reduces both the per-block ×2 and the cumulative 2^(buffer/HL) transient. Stronger option: clamp the timestamp/elapsed-time ASERT CONSUMES (e.g., ASERT sees min(blocktime, prev_mtp+small)) so sustained max-stamping can't inflate cumulative elapsed time. b7 to quantify both.

## Refuted / confirmed
- Liveness under stalls: SAFE (miner clamps into past, self-heals) — CONFIRMED.
- "Unbounded collapse to floor": REFUTED as a realistic-network claim (harness artifact); real bound ~×4 transient (mainnet) pending b7.
