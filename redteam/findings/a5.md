# a5 — edge-case / boundary bugs in PR #214

Real binary regtest, `-test=bip94 -test=matmuldgw` (timewarp always-on like mainnet), drift rule patched active @ height 200, -mocktime. Cleaned up.

## ISSUE 1 — HIGH — Activation-boundary chain-halt (timewarp lower bound ⊥ drift upper bound)
- Lower bound GetMinimumTime (miner.cpp) = max(prev_mtp+1, prev_blocktime − MAX_TIMEWARP[600]). On mainnet BIP94 timewarp is always on (EnforceTimewarpProtectionAtHeight true ∀ height) ⇒ the prev_blocktime−600 term is active.
- Upper bound (consensus time-mtp-too-new, validation.cpp:8446) = prev_mtp + 3600.
- When prev_blocktime > prev_mtp + 4200 (=3600+600), feasible window [prev_blocktime−600, prev_mtp+3600] is EMPTY. UpdateTime sets nNewTime = max(min_time, max_time) = min_time (ABOVE the consensus max) ⇒ node REJECTS its own block "time-mtp-too-new". submitSolution also fails (every ts fails its min or max check).
- REPRO: inject consensus-valid block 199 with time=mtp+5000 (rule inactive at 199); at height 200 node reports mintime > maxtime (INVERSION), generatetoaddress → "AcceptBlock FAILED (time-mtp-too-new)"; chain stuck at 199; advancing mocktime ~9 days does NOT help (bounds depend on tip MTP/time, MTP can't advance without a new block) ⇒ PERMANENT STALL.
- MAINNET: rule activates 118482. Block 118481 NOT subject to rule ⇒ legally stampable mtp+4201…+7200 (within MAX_FUTURE_BLOCK_TIME). Then 118482 unmineable by anyone ⇒ chain halt AT activation height. SELF-PROTECTING in practice: UpdateTime's clamp (active even below activation, ungated) caps honestly-mined 118481 at mtp+3600, so only a custom/attacker miner or clock-skewed external block at 118481 triggers it; organic inversion needs sustained spacing >~840s (unlikely @ 90s target).
- NOTE (orchestrator): post-activation the inversion is IMPOSSIBLE (every capped block N has blocktime ≤ prev_mtp+3600 < prev_mtp+4200), so it's an ACTIVATION-BOUNDARY-only event. Mainnet already passed 118482 cleanly ⇒ DORMANT on mainnet now. BUT reintroduced on any network where the cap is later activated at a height >0 (see b4-1).
- **FIX:** reconcile the two rules so a block can never be unmineable. Effective upper bound = max(prev_mtp+drift, prev_blocktime − MAX_TIMEWARP) (i.e. never cap below the timewarp floor); OR enable the rule from genesis on new networks so there's no unprotected predecessor. The miner clamp + consensus check must agree.

## ISSUE 2 — LOW/MED — product-payload consensus failures reclassified BLOCK_CONSENSUS→BLOCK_MUTATED
- missing/invalid/non-canonical product-payload now BLOCK_MUTATED ⇒ not marked BLOCK_FAILED_VALID, not in m_failed_blocks, InvalidChainFound skipped ⇒ a genuine consensus-invalid block isn't cached as permanently invalid (re-validatable). a5: "semantically wrong" for genuine consensus violations. CAVEAT (cross-ref a3): over P2P this triggers one-shot 100 ban, so NO remote amplification; only local authenticated submitblock re-validates. Net: low, intentional, P2P-safe. Leave as-is unless a cheaper local guard is wanted.

## CHECKED & CORRECT (no bug)
- params.h MatMulFutureBlockTimeLimit overflow guard correct (prev_mtp>INT64_MAX−drift→INT64_MAX); drift≤0/height<0/Height==int32max/fMatMulPOW=false → nullopt; gated vs ungated identical when both active.
- pre-hash gate relocation: same deterministic predicate, no block-validity change (gated fMatMulPOW && !fSkipMatMulValidation && !hashPrevBlock.IsNull()).
- validation ordering: new check additive, independent of time-too-old/BIP94/time-too-new; pindexPrev-null & nHeight overflow guarded at entry (8364/8369) before new check.
- RPC crash surface: CHECK_NONFATAL(pindex_prev) unreachable (all callers null-check); guard throws JSONRPCError not assert; EnforceBlockHexSizeLimit/submitheader-reject/gbt odd-hex all clean. No curtime_clamped overflow.
- below-activation mining clamp divergence: BENIGN — future_mtp_limit_active:true while consensus_active:false below activation; clamped curtime (mtp+3600) is consensus-valid for ALL nodes ⇒ no fork; only makes upgraded miners pick older-than-necessary ts. (This same ungated clamp is what makes Issue 1 self-protecting.)
