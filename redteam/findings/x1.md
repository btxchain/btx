# x1 — EMPIRICAL forge attempt on f3 (SMILE2 CT monomial-challenge soundness) via unshield

Mission: actually CREATE VALUE — submit a SMILE2 CT proof the consensus VERIFIER (`smile2::VerifyCT`)
ACCEPTS but that pays out MORE transparent BTX (`public_fee` = `value_balance`) than the shielded
note(s) it spends. Attack the verifier directly with hand-built / forge-prover proofs; exploit the
2^-8 monomial opening challenge (grind) and/or the `omega`/`framework_omega` `!IsZero()` skips.

Builds on f3 (the OPEN gap), e2 (code-reasoned the 256-monomial; deferred the hand-built malicious
proof as "larger effort"), c5 (impl checks present), i5 (no safe in-house swap). I did what e2
deferred: built the malicious unbalanced proof and fed it to the verifier.

Method: RelWithDebInfo `test_btx` built in docker (`rtx1-tests`, Dockerfile.tests), Alpine 3.22,
isolated `rtX1-` namespace, `/tmp/rtX1`. Red-team harness `src/test/rtx1_f3_forge_tests.cpp` drives
`ProveCT`/`VerifyCT` directly (bypasses the i3/c5 z_sendmany wallet segfault). Two red-team-only
forge hooks added to a PRIVATE copy of `src/shielded/smile2/ct_proof.cpp` (`rtx1::g_forge_skip_balance`,
`rtx1::g_forge_zero_carry`) let the prover build an UNBALANCED proof (claim `public_fee` > true
surplus, force the h2 balance-carry delta to 0 so step-1 passes) — the only way to get a candidate
inflating proof, since the honest prover refuses `sum_in != sum_out + fee` (ct_proof.cpp:2244).
No production assets touched; no foreign containers; no git commit.

## Verifier relations targeted (ct_proof.cpp, VerifyCT @3083)
- STEP 1 balance gate: `h2.coeffs[0..3] == 0` (3122-3133) — the carry `delta = sum_in-sum_out-fee`.
- `omega` weak-opening (M-SIS) accumulator check, GATED `if(!proof.omega.IsZero())` (3579-3586).
- `framework_omega` relation `lhs == framework_omega`, GATED `if(!...IsZero())` (3675-3682);
  `lhs = alpha·(framework_sum - c·f[G] - c²·h2) + bin_check + f[Psi]` (3641-3664) — the
  `f²+c·f` selector-binarity and `c²·h2` balance terms.
- UNCONDITIONAL binding digests: ROUND1_AUX (3478), PRE_H2 (3531), POST_H2 (3665) over recovered
  `aux_tmsg`/`lhs`; per-input compressed-W0 residue eq (3520-3529); combined coin-opening digest
  (3686 → 1944) over the committed input/output amount residues; monomial inversion
  `DivideByMonomialChallenge`/`InvertMonomialChallenge` (3458/3497/3544/3686, 66-100/371).
- All SeedIsPresent-gated seed checks (fs_seed/seed_c0/seed_c/seed_z) — challenges ALWAYS use the
  RECOMPUTED transcript hash, so zeroing a seed never lets the attacker pick a challenge.

## Vectors attempted
<!-- RESULTS APPENDED AFTER RUN -->

## Verdict
<!-- APPENDED -->
