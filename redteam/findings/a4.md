# a4 — split / reorg simulation around activation

Built own correctly-activated image via a NEW regtest CLI arg (`-regtestmatmulmaxfuturemtpdriftheight` / `-regtestmatmulmaxfuturemtpdrift`, 4 small idiomatic edits) → verified future_mtp_limit_height:200. Non-upgraded comparison built from `main` (rule code absent). 4-node + boundary-pair regtest, `-maxtipage=2e9`. Violating blocks crafted on rule-absent `main` nodes via setmocktime(prev_mtp+3600+X)+generatetoaddress (main miner has no clamp/reject). Cleaned up.

## RESULTS
- **Scenario A (minority attacker) ✅:** evil `main` node mined height-206 block drift=5600s. Honest h1/h2 (rule@200) stayed at 205, tip unchanged; rejected at HEADER ("time-mtp-too-new") and never relayed onward. Intended protection holds.
- **Scenario B (non-enforcing majority) ✅:** non-upgraded `nonup` adopted the violating chain; evil+nonup reached 210 (5 blocks ahead, more work). Honest h1/h2 stranded at 205, never reorged onto the invalid higher-work chain (getchaintips doesn't even track it as a candidate). HEALING: mined 7 compliant honest blocks (→212); honest chain overtook; ALL nodes (incl nonup, which BlockDisconnected 206/207/208) converged on honest tip 212. Soft fork heals once enforcing chain has most work.
- **Boundary ✅ off-by-one-correct:** IsMatMulMaxFutureMtpDriftActive flips EXACTLY at height 200 (199 inactive→accepted by upgraded; 200 active→rejected). Strict `>`: drift==3600 ACCEPTED, 3601 REJECTED. Rule uses NEW block height vs prev MTP; first subject block is height 200.

## NEW / observations
- **Mining-policy vs consensus flag divergence (LOW/cosmetic):** future_mtp_limit_active / future_mtp_mining_policy_active report true at heights <activation (driven by height-INDEPENDENT MatMulFutureBlockTimeLimit) while future_mtp_consensus_active is correctly false. Stricter-than-consensus self-restraint (benign, same as a5), but the divergence could confuse miner operators reading the policy object.
- **Stranding risk real & by-design (MED):** minority enforcement ⇒ upgraded nodes wedge on lower-work chain until honest hashpower regains most work. Actionable: deploy only with majority-hashpower enforcement.

## Useful artifact for the PR
- a4's regtest CLI-arg approach (4 edits) is a clean way to make the rule activatable/testable in regtest — aligns with b4-1 (enable on all networks). Consider folding into the hardening PR so the rule is natively testable.

## Refuted / robust
- The soft-fork tightening behaves correctly; boundary is clean; no off-by-one; heals when enforcing chain leads. The split risk is the (already-known) operational deployment caveat, not a code bug.
