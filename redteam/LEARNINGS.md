# LEARNINGS (curated by orchestrator; agents READ this before attacking)

Updated continuously as waves report. Newest facts on top.

## ✅ w2 — SCOPING: the active CT value/selector side has EXACTLY ONE hole (x1c); everything else SOUND
- 15 forges (real output) across every "trust-a-prover-value" site in VerifyCT's VALUE/SELECTOR relations. ONLY accept = the x1c balance carry (reproduced 2 ways: drop-carry rtW2-X, direct-zero-h2 rtW2-D0). ALL others REJECTED: selector-binarity (non-binary slot), one-of-many ring membership (two-hot → spend 2 members), committed-amount-vs-proven (commit 100 prove 200), omega/framework_omega gate-zeroing bypass.
- WHY sound: violations caught at STEP 1 (h2 carry) OR STEP 11 (unconditional post-h2 framework binding digest, which binds f[selector]/f[x]/amount-residues via the masking responses BEFORE the gated framework_omega eq — so zeroing gates doesn't help). x1c's carry-zero is NOT a master key (only nulls fee carry, not live_y_sum). Balance is the LONE term the prover moves freely (lives only in proof.h2).
- ⇒ CONSOLIDATED CONFIRMED consensus-reachable shielded holes = **TWO, both bounded + characterized**: (1) x1c BALANCE carry [value side, w2-confirmed singular]; (2) w3-1 SERIAL↔KEY nullifier [serial side]. Membership-vs-TREE = enforced OUTSIDE VerifyCT (anchor check, sound). ⇒ the FIX is TWO specific missing independent equations, not an open-ended rewrite. (External audit still needed for the lattice PRIMITIVE soundness + bridge + anything the forge harnesses didn't cover, but the immediate consensus-reachable bleed is bounded to these 2.)

## ████████ CRITICAL #2 — NULLIFIER FORGEABLE → DOUBLE-SPEND (w3, EMPIRICAL) ████████
- **CONFIRMED (verify=1, work factor 1):** overwrite proof.serial_numbers[0] with an attacker value DECOUPLED from the spent key ⇒ VerifyCT ACCEPTS. Two distinct serials for one note ⇒ BOTH accepted ⇒ DOUBLE-SPEND. Same bug class as x1c.
- ROOT CAUSE (w3-1): on the live m=1 path, VerifyCT uses proof.serial_numbers ONLY for a size check + FS-transcript append (ct_proof.cpp:3277) + a log line — NEVER an arithmetic check against the key opening z0. The serial↔key relation (⟨b_sn,z_0⟩=⟨b_sn,y_0⟩+c_0·sn) is never closed (the serial garbage-commitment t'_1 is only in the DEAD legacy branch). On-chain nullifier = ComputeSmileSerialHash(proof.serial_numbers[i]) taken VERBATIM (verify_dispatch.cpp:223, v2_proof.cpp:2003); spend-auth compares two values both derived from the SAME forged serial. ⇒ **e3's "a different serial needs a different valid s" is EMPIRICALLY FALSE on the live path** (code-reasoning wrong again, like e2/c5 on balance).
- ★ TURNSTILE BACKSTOP (answers "is x1c exploitable / why no inflation on metrics"): the verifier has NO pool context. The outer turnstile pool≥0 is a ONE-SIDED aggregate ceiling: for x1c (balance) it bounds extraction to the LEGIT POOL TOTAL ⇒ **x1c IS end-to-end exploitable — an attacker can DRAIN the shielded pool (up to its size), not infinite-mint**; for w3-1 (double-spend) each spend is individually balanced so the pool ceiling gives ZERO protection ⇒ fully exploitable. ⇒ "no inflation on metrics" = NOT-YET-EXPLOITED (we found it first), NOT unexploitable.
- FIX: the live m=1 CT relation is missing TWO independent equations — balance-carry recompute (x1c/y1) AND serial↔key binding (⟨b_sn,z_0⟩ − c_0·serial == w_sn). ⇒ **y1's balance fix is NECESSARY-BUT-NOT-SUFFICIENT; the nullifier binding must be added too.** Both same fix shape (verifier independently recomputes the value). ≥2 structural holes ⇒ DO-NOT-SHIP unshield reinforced; assume MORE (w2 CT-internals + a full audit). Apply un-gated (rejects only already-invalid proofs).

## ████████ CRITICAL #1 — SHIELDED INFLATION: VerifyCT accepts forged-balance proofs (x1c, EMPIRICAL) ████████
- **CONFIRMED with real verifier output** (rtx1_05): `smile2::VerifyCT` (CONSENSUS shielded verifier) ACCEPTS a proof declaring public_fee=50 on a truly balanced in=100/out=100 spend (real surplus 0) ⇒ **50 sat minted from nothing. verify=1, DETERMINISTIC, work factor 1** (NO grind, NO crypto break). rtx1_08: still accepts even with omega/framework_omega zeroed (gates irrelevant).
- ROOT CAUSE = **structural soundness hole in the balance relation** (NOT the f3 monomial/M-SIS question — that's MOOT here): VerifyCT NEVER independently recomputes the balance carry `delta = sum_in − sum_out − public_fee` from the committed amounts; it only checks `proof.h2.coeffs[0..3]==0` and TRUSTS the prover's h2. Attacker drops the −50 carry ⇒ STEP-1 passes. The PRE_H2/POST_H2/coin-opening binding digests bind the prover's OWN FORGED h2 to itself ⇒ don't catch it (refutes e2's "digests pin the relation" + c5's "balance present"). public_fee is bound only into the transcript/seed (so lying about fee on an HONEST proof is caught — rtx1_06 rejects), but a forge prover builds the WHOLE proof consistently with fee=50 and just omits the carry (2-line patch: g_forge_skip_balance @2254 + g_forge_zero_carry @2866).
- PERMISSIONLESS at consensus: VerifyCT runs at MEMPOOL admission ⇒ attacker hand-builds a raw tx with their own patched prover; wallet gate irrelevant.
- ★ HONEST CAVEAT (precise severity): x1c DEFINITIVELY proved the PRIMITIVE VerifyCT accepts the forged CT proof. The OUTER tx checks (CheckTxInputs value_balance fold, turnstile pool≥0, MoneyRange, statement-digest) bound REALIZED transparent egress to ≤pool & ≤MAX_MONEY but do NOT re-derive shielded balance — they TRUST the CT proof. So an attacker declares value_balance=true_surplus+Δ and the fold credits Δ transparent sat as long as the running pool covers it (drained from honest depositors). x1c ASSESSES end-to-end mint succeeds but did NOT run the full-node submission — **ONE empirical step remains** (submit a forged raw unshield through MemPoolAccept/ConnectBlock; now unblocked by i3). Verifier-level break = DEFINITIVE; end-to-end mint = assessed-yes/pending-1-test.
- IMPLICATIONS: (1) **self-service unshield feature = DO NOT SHIP** (turns this into a permissionless high-volume inflation surface). (2) f3 is no longer "candidate-CRIT pending monomial audit" — it's an EMPIRICAL verifier break needing no crypto break ⇒ strictly worse, and the LIVE shielded pool (consensus, active since shielded launch) is exposed NOW. (3) URGENT: a turnstile/value-pool imbalance MONITOR on mainnet to detect active exploitation.
- FIX (x1c, may be MORE tractable than i5's challenge-swap — it's "add a MISSING equation," not redesign the challenge): make VerifyCT INDEPENDENTLY recompute the carry and require `proof.h2 == g0 + live_y_sum + carry(sum_in − sum_out − public_fee)` over the RECOVERED committed amounts + public_fee (an independent value-conservation constraint the prover can't satisfy with a dropped carry); verify by RE-RUNNING rtx1_05 → must REJECT. Consensus change, gate @126400; still warrants the external audit (C-002) but the structural fix is empirically checkable.


## ███ HIGH (was CRIT; e5 recalibrated) — MatMul PoW amortization break (e1, CONFIRMED by e5+f1) ███
- SEVERITY: e5 (skeptical verifier) recalibrates **CRIT → HIGH (bordering CRIT)**: it's an UNDETECTABLE work-asymmetry — NO invalid block / inflation / halt / forgery; self-limiting under ASERT once public. Damage = breaks the useful-work/ASIC-hardness premise + transient SECRET-advantage / cheap-51% window on a high-value chain. Still the most severe finding; still needs the hard-fork fix. CONFIRMED independently by BOTH e5 (bit-identical words n=64/128/192; 3.35× regtest, 13.2× mainnet) and f1 (1.00× post-fix).
- **The "Ω(n³) field work per nonce attempt" claim is FALSE; true per-nonce cost is Θ(n²·r) ⇒ ~12.8× cheaper mining on mainnet (n=512,r=8), undetectably.** Orthogonal to b1 (forgery): produces FULLY-VALID, bit-identical, honest-looking blocks while doing far less work per nonce than the honest reference miner. e5: deployed miner does dense O(n³)/nonce on EVERY backend (CPU + CUDA fused kernel); base-cache only skips re-deriving A,B from seed, NOT the product; devs ship Denoise() (same decomposition) but only verifier-side — had the pieces, missed the forward use.
- ROOT CAUSE (VERIFIED in code): `DeterministicMatMulSeed(prev_block_hash, height, which)` = SHA256(prev‖height‖which) — **NONCE NOT INCLUDED** (pow.cpp:47-52), consensus-enforced "bad-matmul-seeds" (validation.cpp:8385). ⇒ base matrices A,B and their dense product A·B are CONSTANT across the entire grind at a tip. The honest path recomputes A'·B' (O(n³)) per nonce; but A'B' = A·B + (A·F+E·B+E·F) where the per-nonce noise term is GLOBALLY rank ≤ 3r ⇒ precompute A·B ONCE per tip, then per nonce only the low-rank correction O(n²r) + compress O(n²). Digest bit-identical (e1 empirically verified, field-exact, n=16/32/48/64).
- NUMBERS: mainnet honest 138.7M vs optimal 10.8M mults/nonce (12.8×); one-time precompute 134M (<5% of one block honest cost). regtest 3.4×. Asymptotic ≈ n/(6r). The devs' own matmul_reuse_bench measures a BLOCK-LOCAL reuse (3× slower red herring) — they missed the GLOBAL low-rank collapse.
- IMPACT: whoever implements this out-mines honest miners ~12.8× (secret advantage until public; then difficulty re-adjusts but the matmul-WORK thesis the chain rests on is broken; enables cheap hashrate dominance / 51%).
- FIX (e1, REFINED by f1): fold the nonce into the seeds so A·B changes every attempt. ★ HARD FORK (amortized blocks indistinguishable ⇒ cannot soft-fork) ⇒ own coordinated flag-day.
  - **f1 CONFIRMS e1** (independent field-exact reproduction) AND that the fix CLOSES it: under per-nonce seeds, A·B distinct, A/B full-rank, cross-nonce diffs full-rank, no shared sub-blocks ⇒ best-attacker == honest = **1.00×**. No residual dense-product amortization axis. No memory-hardness (working set ~8MB; the FromSeed cap-8 cache actually ENABLES e1). Sigma lottery symmetric (E[matmuls/block]=2^eps=262144 mainnet), not a shortcut.
  - **f1-A caveat (fix-design):** naive `seed=SHA256(...‖nonce)` regenerates A,B via 1 SHA256/ELEMENT = Θ(n²) SHA/nonce ⇒ shifts PoW weight onto SHA256 (most ASIC-favorable): 28.7% of per-nonce cost @ n=512, 77.7% @ n=64. ⇒ DON'T regenerate per-element-SHA; expand per-nonce matrices via a FAST counter-mode PRG (AES-CTR/ChaCha) so MATMUL stays ≫ generation.
  - **f1-B caveat (fix-scope):** the getmatmulservicechallenge subsystem (rpc/mining.cpp:1551-1555) uses the SAME nonce-free seeds and is independently amortizable (advertises publicly_precomputable_before_parent_seen / winner_knows_next_seeds_first) ⇒ the fix MUST extend there too (ties to d4 service-challenge audit).
  - e5 still adversarially verifying severity/exploitability; raising r→n/6 is a blunt alt (10× honest cost).
  - **h1 IMPLEMENTED + CONFIRMED the fix** (live regtest, -test=matmulstrict): per-nonce seeds ⇒ A·B distinct/nonce; old-seed block → bad-matmul-seeds REJECT; honest==attacker cost; GENESIS compatible (seed-binding skipped for null prevhash ⇒ no genesis re-mine). But NAIVE form has REGRESSIONS: (a) ★ pre-hash prefilter +1.54× (sigma now needs per-nonce seeds first) — AVOIDABLE: keep the cheap sigma GATE on nonce-INDEPENDENT seeds, only the dense MATMUL uses per-nonce seeds (winners only); (b) +22% per-accepted-nonce seed-regen (n=512); (c) g_from_seed_cache + cross-nonce GPU batch pipeline (SubmitMatMulDigestPreparedBatchForMining) BREAK — must force batch_size=1 (disables cross-nonce GPU amortization — that amortization WAS e1; losing it is ~aligned w/ per-nonce GPU-hard intent but a throughput hit); (d) ~15% slower e2e regtest. Must also patch getmatmulchallenge/profile + genesis tool + service-challenge (f1-B).
  - h1 FULL report adds integration specifics for i1: (i) **getmatmulchallenge RPC pins a single nonce-0 seed over the whole noncerange ⇒ breaks external miners (nonce≠0→reject)** — must nonce-fold there too; (ii) **solvematmulservicechallenge/DeriveMatMulServiceSeed is nonce-independent ⇒ service-challenge PoW STILL e1-amortizable** (f1-B) — same fix; (iii) ~16 batch-pipeline pow_tests assert per-batch reuse (the e1 weakness) ⇒ FAIL under the fix, must be rewritten to assert per-nonce regen; (iv) batch_size forced to 1 (cross-nonce GPU batch amortization disabled — aligned w/ per-nonce GPU-hard intent). e1-fix is otherwise sound: defeats e1 (old-seed block REJECTED bad-matmul-seeds, A·B distinct/nonce), determinism preserved, genesis exempt.
  - ⇒ PREFERRED FIX (combine h1 "better fix" + diagonal-scaling): keep sigma gate seed-independent (no prefilter regression); make the DENSE PRODUCT nonce-dependent via cheap O(n) per-nonce diagonal scaling D_nonce on A,B-from-fixed-seeds (A·(D·B) full-rank-distinct/nonce, NOT amortizable, NO per-element SHA ⇒ no ASIC-friendly regression, A,B stay cacheable). Implementation to A/B-test diagonal-scaling vs h1's per-nonce-seed variant for least honest-cost while fully closing e1.

## f2 — no new matmul algo break; GPU/ASIC asymmetry is a DESIGN truth (agent f2)
- NTT shortcut REFUTED (p=2^31-1 2-adicity=1 ⇒ no order-512 root of unity; Mersenne NTT-hostile; NTT≠dense GEMM). Matrix structure REFUTED (FromSeed = per-entry SHA256 oracle, full rank, no Toeplitz/circulant). Strassen LOW (~1.5× constant; moot today since e1 amortizes A·B; BECOMES per-nonce-relevant after the e1 fix).
- **GPU-favorability is INTENDED DESIGN, NOT a flaw (per maintainer).** The goal IS pure raw GPU compute, GPU-HARD across all brands (NVIDIA/AMD/Apple), explicitly NOT memory-hard and NOT ASIC-friendly. Dense INT32 GEMM is brand-AGNOSTIC GPU work ⇒ aligns with intent (no single-architecture ASIC edge beyond a general tensor accelerator, which is GPU-class). f2's Q4 is therefore reframed: NOT a finding. The only legit watch-item is whether any SINGLE-vendor ASIC could dominate general GPUs — dense GEMM doesn't favor one vendor, so OK.
- ★★ HARD CONSTRAINT on the e1 fix (regression guard): the fix MUST keep the per-nonce work as pure dense-GEMM (GPU-hard, brand-agnostic). It must NOT add per-element SHA256 (f1-A's naive regen — SHA is ASIC-FRIENDLY ⇒ REGRESSION) and MUST NOT introduce memory-hardness (REGRESSION). PREFERRED FIX: make A·B nonce-dependent via a CHEAP O(n²) structured perturbation that forces a fresh FULL-RANK dense product per nonce with negligible non-matmul overhead — e.g. a per-nonce diagonal scaling D_nonce so honest computes A·(D·B): one extra O(n²) row-scale (no hashing), product is full-rank-different per nonce ⇒ NOT low-rank-amortizable (A(D1−D2)B is full-rank diagonal), derivation of D from nNonce64 is O(n). Avoids both the SHA-dilution (f1-A) and any memory-hard regression. → h1/implementation to validate this variant vs naive seed-rehash.

## ✅ i3 — z_sendmany SIGSEGV ROOT-CAUSED + FIXED (agent i3) — benign, NOT remote
- ROOT CAUSE = **STACK OVERFLOW** (not OOB/UAF/uninit). ASan: stack-overflow at smile2::TryProveCT prologue (ct_proof.cpp:2215) on a b-httpworker thread. TryProveCT (~840 lines, dozens of by-value SmilePoly/NttForm temporaries ~1KB each) = ~128KB frame; Alpine/musl non-main thread stacks default to 128KB ⇒ overflow. (z_shieldfunds works — no TryProveCT; z→z crashes.) musl-specific (glibc 8MB thread stacks wouldn't crash — why prod node didn't, regtest musl did).
- SEVERITY MED, sender/WALLET-LOCAL only — **NOT remote-reachable**: VerifyCT (consensus verifier) frame ~64KB < 128KB worker stack ⇒ poison block does NOT reproduce ⇒ c4/c2 "verify robust" CONFIRMED. (Not a memory-corruption/exploitable bug — pure stack-depth availability.)
- FIX (VERIFIED): run the prover on a dedicated 8MB-stack pthread at the CreateSmileProof call site (wallet_bridge.cpp); joins, forwards exceptions, inline fallback. No crypto change. Fixed build: 2× z→z z_sendmany SUCCEED + mined (note spent, tree 37→44, VerifyCT passed), node up, ASan-clean, verifychain=true. UNBLOCKS v1 unshield + x1/x2/x3 live shielded tamper tests.
- ★ i3-vs-x2 TENSION on the VERIFIER (integration requirement): i3 measured VerifyCT ~64KB ⇒ fits 128KB musl stack ⇒ NOT remote-reachable. x2 (gdb + A/B stack-size control on musl) says the verify path CAN overflow on the constrained mempool/CCheckQueue/block-connect threads ⇒ candidate REMOTE-CRASH DoS on the shipped Alpine/musl binary (poison tx/block). UNRESOLVED on the margin, but the FIX is identical either way ⇒ **INTEGRATION MUST give BOTH prove AND verify threads an explicit large stack (or heap-allocate the big SmilePoly/NttForm buffers)** — not just the prover call site i3 patched. (If it matters, a 10-min musl repro settles whether it's wallet-local MED or remote HIGH; the fix covers both.)

## ★ e2 — Frozen-Heart REFUTED; NEW MAX_MONEY gap (MED); f3 SOFTENED (agent e2)
- **No Frozen-Heart / weak-FS forgery — CONFIRMED empirically (ASan/UBSan harness driving ProveCT/VerifyCT directly, bypassing the wallet segfault).** Both proof systems hash the FULL public statement + EVERY prover commitment into the challenge; tampering any public input (coin commitment, value_balance/fee, anon_set pk, shared A, input ring) or lifting a proof onto another statement → ALL REJECTED (tests e2_a1–a5,b3 pass clean). The round1_aux/pre_h2/post_h2 binding digests are recomputed + re-checked UNCONDITIONALLY (closes the adaptive gap; the SeedIsPresent/omega conditionals c5 flagged are NOT holes).
- **★ NEW (MED, concrete fix): smile2 CT does NOT bound output amounts to MAX_MONEY.** Empirically a balanced CT proof with a 2^60-sat output (~550× MAX_MONEY) VERIFIES (base-4 32-slot encoding allows 2^64−1; neither ProveCT nor VerifyCT checks MAX_MONEY; SMILE send forces output_range_proofs empty, v2_proof.cpp:942). NOT a standalone inflation break (c5's value_balance/pool/egress MoneyRange checks stop realized >MAX_MONEY value entering/exiting), but a missing range invariant that could combine with another bug. FIX: add per-output amount ≤ MAX_MONEY to the smile CT relation/validation (mirror matrict's static_assert 2^VALUE_BITS>MAX_MONEY, VALUE_BITS=51). → PR2, gate 126400.
- **★ f3 SOFTENED by e2:** e2 rates the smile2 monomial ±X^k (|C|=256) opening challenge **LOW/defense-in-depth, NOT a ~1/256-grindable hole** — it governs ONLY BDLOP commitment-opening BINDING; the VALUE relation is pinned to negligible error by the UNCONDITIONAL pre/post-h2 binding digests + high-entropy alpha/gamma/opening challenges. So practical exploitability is likely LOW (consistent with x2: nobody can craft a passing false-value proof). f3 vs e2 = same code, different severity calls (f3 candidate-CRIT on the unproven reduction; e2 LOW on the backstopped value relation) ⇒ EXTERNAL AUDIT resolves it; x1's empirical forge attempt is the tiebreaker.
- **i3 hint (e2):** the ProveCT/VerifyCT lattice relation is ASan-CLEAN over 100s of calls ⇒ the z_sendmany SIGSEGV is in the WALLET BRIDGE / spend-path assembly, NOT the lattice relation. Needs an ASan-instrumented DAEMON build. BUILDER NOTE: SHA256 SSE41/AVX2/SHANI asm faults under ASan at startup self-test ⇒ disable HAVE_SSE41/HAVE_AVX2/HAVE_X86_SHANI for ASan builds.

## ███ f3 — SHIELDED SOUNDNESS gap (HIGH, candidate-CRIT inflation/forgery) — agent f3 ███
- Active shielded = **SMILE2** (based on SMILE, Lyubashevsky-Nguyen-Seiler CRYPTO'21, BDLOP commitments, ring Z_q[X]/(X^128+1), q=2^32-959). Legacy = MatRiCT/MatRiCT+.
- **F3-1:** SMILE2 CT proof gates commitment OPENINGS with a MONOMIAL FS challenge c=±X^k — only **256 values (2^-8 per-opening knowledge error)**. Statement relations use ~2^128 slot challenges, but the OPENING (extractor basis) does not. Sound ONLY IF a tight M-SIS reduction holds for this exact monomial transcript — **UNPROVEN, flagged by BTX's OWN auditors as C-002, deferred** w/o the security estimate. If the reduction is loose ⇒ forge a CT opening at ~2^8 work ⇒ INFLATION. (e1 tactic: perf-driven param swap — monomial chosen so ‖c·r‖∞≤1 enabling 7-bit responses — with proof ASSUMED.) Not demonstrably broken; the OPEN gap IS the finding.
- **F3-2 precedent:** repo ALREADY shipped ~23-bit scalar challenges in LEGACY balance/range into consensus, caught+fixed (→>200-bit polynomial) only in audit Round 3. SAME bug class, fixed on legacy, STILL LIVE on active SMILE2.
- SOUND: rejection sampling (prover-side, no leak), lattice FS (full-transcript-bound, no Frozen-Heart), params paper-faithful (threat is SOUNDNESS not hardness/BKZ).
- FIX: external audit to complete C-002. ★ i5 VERDICT (verified by static dataflow): the conservative monomial→polynomial SWAP is NOT safely implementable in-house — the SMILE2 verifier DIVIDES BY the challenge (DivideByMonomialChallenge→InvertMonomialChallenge, ct_proof.cpp:82/371) which THROWS for any non-monomial challenge ⇒ a polynomial challenge breaks EVERY honest verify ⇒ chain split. Legacy SampleChallenge is a different ring/proof-system (affine, never inverts) ⇒ no reuse. σ/β/extractor re-derivation IS the deferred C-002 cryptographer work; guessed consensus crypto params are unacceptable. Parallel-repetition (κ=16→2^-128) rejected (~16× blowup + doesn't fix the loose-reduction failure mode). ⇒ **NO safe in-house f3 fix. Options: external audit (C-002, 5 deliverables in i5.md §7) OR an OPERATIONAL constraint on the active shielded send path.** Height-gating mechanics are fine (nMatMulHardeningHeight gate) but the post-gate branch can't be filled safely.
- ★★ CONSEQUENCE for the headline FEATURE (v1): self-service unshield turns f3 into a PERMISSIONLESS, operator-free, high-volume inflation surface. Since f3 CANNOT be fixed in-house, shipping self-service unshield ENABLED in 0.30.3 = amplifying an unproven-soundness inflation risk. ⇒ DEFER/GATE the self-service unshield (keep operator-mediated, or add hard operational limits) UNTIL the external C-002 audit clears f3. Decision needed.

## e3 — shielded membership/nullifier/privacy IMPL sound; z_sendmany segfault TRIPLY-confirmed (agent e3)
- No new double-spend/spend-nonexistent/nullifier-replay/privacy break at impl level. Nullifier = note-unique key-image nf=SHA256(⟨b_sn,s⟩), s per-note, Schnorr-bound, canonicalized (no malleability), payload==proof nullifier (no swap), whole-tx+chain bound (no replay). Membership = domain-sep CR SHA256 Merkle, fail-closed anchor. Ring (MatRiCT) fork-disabled w/ version gates. Privacy = ML-KEM-768 KEM-DEM fresh per-note randomness; post-Monero-audit decoy selection. RESIDUAL = soundness of the smile2 one-of-many relation (does it force the SAME hidden index across key/coin/membership/serial subproofs — extractor question → f3 + external audit). NEW: z_sendmany(z→z) SIGSEGV on a 3rd independent build (smile2 CT prove/anon-set path) ⇒ MED, shielded sends broken, BLOCKS empirical tamper tests (c5+e3+e3 = triply confirmed) → ASan root-cause.

## d4 — service-challenge consume-once SOUND; eviction-DoS (agent d4)
- The matmul service-challenge = OFF-CHAIN anti-spam gate (NOT coin issuance) ⇒ no reward-theft. Consume-once SOUND: double-redeem, 16-way race, restart-replay, downgrade-tamper, cross-binding all REJECT (mutex+file-lock+atomic write+challenge_id binds all fields + b1-sound matmul verify).
- **NEW d4-1 (MED) registry eviction-DoS:** 10k-entry LRU evicts lowest issued_at REGARDLESS of redeemed/pending (mining.cpp:2694). ~10k issuances (~2.4min, NO PoW/coin) sweep out legit PENDING challenges ⇒ victims' solved proofs → unknown_challenge. FIX: never evict unredeemed/unexpired (evict redeemed/expired first); rate-limit issuance/client.
- d4-2 (LOW): verifymatmulserviceproofs returns valid:true for already-redeemed (app gating on `valid` not `redeem` = replay). d4-3 (LOW): issuance not chain-guard-gated. PLUS f1-B: service-challenge uses the SAME nonce-free seeds ⇒ ALSO e1-amortizable ⇒ the e1 hard-fork fix must extend here.

## d3 — shielded full-chain-rebuild DoS amplification (agent d3, MED→HIGH)
- A CHEAP mempool tx with a recoverable shielded-proof reject (notably `bad-shielded-ring-member-position`, a structural check on attacker-controlled ring_positions, v2_proof.cpp:2048) auto-invokes `RebuildShieldedState` (full genesis→tip replay, the ~25-min op) from MemPoolAccept **with NO PoW required** (validation.cpp:3735). Dedup keyed on <tree.Root,tree.Size,registry.Root> caps to one rebuild per shielded-state but RE-ARMS on every new shielded block ⇒ ~1 free O(chain) CPU/IO stall per shielded block via one cheap tx. FIX: restrict self-repair to block-connect/startup only; bounded incremental re-sync from last checkpoint; drop "position absent" rejects from the recoverable set on the mempool path. (header-flood, O(n³) fallback, reorg/index costs all REFUTED/bounded — per-peer 32/min + global 512/min verify budget.)

## e4 — PQ signatures ROBUST; c3 RNG DOWNGRADED HIGH→LOW
- No forgery/key-recovery/replay. BTX Dilithium signing is FIPS-204 HEDGED (nonce seed = CRH(sk‖rnd‖mu)) ⇒ even zeroed/attacker RNG does NOT leak the key (no ECDSA-style nonce reuse). c3's fail-open /dev/urandom→memset(0) is UNREACHABLE in production: callers (pqkey.cpp) always pass ≥128B entropy from Core GetRandBytes which is itself fail-closed (abort). ⇒ **re-rate c3 RNG HIGH→LOW** (latent code-quality; fail-closed fix still warranted as defense-in-depth). Domain separation (P2MR/CSFS distinct tags), dilithium byte-faithful to pinned upstream, z-norm/hint malleability enforced, params standardized (ML-DSA-44 L2, SLH-DSA-SHAKE-128s). LOW: delete zero-fill fallbacks (abort); error on insufficient injected entropy. INFO: no genesis/fork-id in P2MR sighash (optional). Lead: ref code not constant-time (wallet-side only).


## Confirmed facts about the target (ground truth)
- **ASERT half-life (mainnet) = 3,600 s**, set directly (no retune/upgrade active; those heights = INT32_MAX).
  Source: src/kernel/chainparams.cpp MAIN. ⇒ ASERT target ×= 2^((elapsed-expected)/3600).
- **PR #214 future-drift cap = 3,600 s == one half-life.** So a single max-drift block can move difficulty ~×2.
  cap rule: at height ≥ nMatMulMaxFutureMtpDriftHeight, block.time ≤ prev_block_MTP + 3600, else reject "time-mtp-too-new".
- **MAX_FUTURE_BLOCK_TIME = 7,200 s** (src/chain.h). Block also rejected if block.time > local_clock + 7200 ("time-too-new").
  ⇒ chain time cannot exceed real time by >2h. This BOUNDS the MTP-ratchet's absolute lead (the external proposal missed this).
- Mainnet activation height 118,482 (≈6h window from tip 118,242 at deploy). Regtest: rule OFF by default
  (height=INT32_MAX); the `*-regtest-ruleon` image enables it at height 200, drift 3600.
- Regtest: pre-hash epsilon DISABLED (nMatMulPreHashEpsilonBits=0); product-digest ACTIVE at height 0;
  fMatMulRequireProductPayload=true. Mainnet: pre-hash epsilon 18 bits since height 50,000; product-digest since 61,000.
- Freivalds verification: sigma = matmul::DeriveSigma(block) [from header fields + nonce] seeds
  noise::Generate(sigma, n, noise_rank) which drives the challenge. noise_rank is a global consensus param, not per-block.
  ⇒ challenge is deterministically derived from grindable block data — see vector B1.

## Live mainnet observation (post-deploy, height ~118.5k)
- Timestamps track real time, sitting 2,457–3,390 s UNDER the cap (nobody gaming toward the ceiling).
- Difficulty oscillating ±10%, responding normally to interval variance; intervals mean ~101s/median ~59s (target 90s).
- No "time-mtp-too-new" rejections, single active tip, no fork. Problem appears to be resolving.

## ★ c3 SUPPLY-CHAIN — confirmed HIGH crypto RNG defect + deps (agent c3)
- **HIGH (VERIFIED by orchestrator): fail-OPEN RNG in PQ keygen** — src/libbitcoinpqc/src/ml_dsa/utils.c custom_randombytes_impl: /dev/urandom open-fail → memset(out,0) (line 59-61); short read → zero-fill remainder (67-70); injected-entropy path WRAPS/REUSES bytes (79-97). ⇒ predictable/zero or nonce-reused ML-DSA(Dilithium) keys. Realistic exposure MED (RNG rarely fails; wallet-local keygen) but MUST-FIX (fail-closed: abort on RNG failure; error on short entropy; no reuse). VERIFY it's the production keygen path (vs overridden by Core GetStrongRandBytes) — but fix regardless.
- HIGH (inherent): src/matmul/ + src/shielded/ (smile2, ringct/matrict, lattice) = 100% bespoke "BTX 2026" consensus-critical crypto, no upstream/audit/CVE history ⇒ recommend EXTERNAL crypto audit (documentation item, not a code fix).
- MED: libbitcoinpqc is a fork of an experimental personal repo (self-declared "not production-hardened"), consensus-reachable via script/pqm.cpp PQ verify; provenance mislabeled (Cargo repository URL 404s). dilithium pin 444cdcc ~current; VERIFY sphincs/kyber pins.
- MED: depends/ sqlite 3.46.1 has CVE-2025-6965 (7.2)/CVE-2025-29087 (local wallet DB; low exploit); shipped Alpine container already 3.49.2 (mitigated). depends/ expat 2.4.8 + freetype 2.11.0 (incl actively-exploited CVE-2025-27363) = GUI-only, N/A headless.
- LOW build-chain: CI curl|tar no checksum; Dockerfile apk unpinned (non-reproducible); kyber/ml-kem no recorded upstream commit+hash. CLEAN: zeromq 4.3.5, libevent 2.1.12, boost 1.81; depends fetches SHA256-pinned; no .gitmodules (all in-tree → no submodule-takeover).
- **s2 vendored bitcoin libs = NO BACKDOOR (clean).** ★ secp256k1 BYTE-IDENTICAL to upstream v0.6.0 + Knots base (field/scalar/ecmult/ecdsa/group/context/RNG/gen-tables) — ZERO non-cosmetic changes. All std primitives (AES/ChaCha20/Poly1305/HMAC/RIPEMD160/SHA1/256/512/3/SipHash/crc32c) byte-identical; minisketch matches upstream d1e6bb8; SHA256/HKDF differ only by memory_cleanse destructors (no output change). ml-kem edits = constant-time hardening (security-POSITIVE). ethash/kawpow vendored but DISABLED (fKAWPOW=false, height INT_MAX). secp256k1 v0.6.0 not behind any advisory. LOW/benign: S2-1 leveldb bytes_written stat regression (log-only, no consensus); S2-2 minisketch stale (relay); S2-3 ml-kem/ethash unrecorded pins. Fork base = Bitcoin Knots v29.2.knots20251110.
- **s3 PQ + build-chain:** PQ provenance CLEAN — dilithium@444cdcc (current), sphincs+@7ec789a byte-IDENTICAL to upstream, ml-kem = faithful PQClean ML-KEM-768 + EXTRA constant-time hardening (stronger, no backdoor); ml-kem NOT consensus-reachable (wallet-only). depends/ fully SHA256-pinned canonical; **release trust root = contrib/guix (reproducible, GPG-attested — strong)**. GAPS: ★ released binary (Guix→depends) ships sqlite 3.46.1 = CVE-2025-6965 (NOT mitigated by Alpine; that's dev-Docker only) → bump depends sqlite ≥3.50.2 (LOW, wallet-local); CI curl|tar shellcheck/mlc no-checksum (injection vector); Dockerfile apk + alpine:3.22 unpinned (LOW-MED, dev only); ml-kem SHA unrecorded (LOW); libbitcoinpqc Cargo URL 404 (dev-only Rust layer). NO consensus-reachable supply-chain risk.
- **s1 std-libs = CLEAN as shipped (no open reachable CVE).** Shipped = Alpine 3.22 apk (dynamic) via contrib/docker/Dockerfile; depends/ is Guix/release-only (ships nowhere in container). Shipped Alpine sqlite **3.49.2-r1 IS the backported fix** for CVE-2025-6965/29087 (resolves c3's open item). boost(1.84 alpine)/libevent/zeromq/leveldb1.22/crc32c/zlib1.3.2 all clean. All depends tarballs SHA256-pinned canonical, no takeover. TOP gap: **unpinned apk versions = non-reproducible** (pin pkg=ver-rN + repo snapshot). leveldb/crc32c are in-tree subtrees → record upstream commit (→ s2).

## ███ x3 — STRUCTURAL BLOCKER: self-service unshield needs a HARD FORK, not a wallet re-enable (CORRECTS v1) ███
- ★★ **v1 was WRONG that unshield is a non-consensus 1-screen re-enable.** Code chain (x3, pr-214): post-fork a V2_SEND with shielded spends is FORCED to `SMILE_COMPACT_POSTFORK` (RejectDirectSendEncodingForFork) which **ELIDES value_balance and reconstructs `value_balance = fee` on the wire** (v2_bundle.h:687/739; SendPayload::IsValid HARD-REQUIRES value_balance==fee, v2_bundle.cpp:749). The builder sets value_balance=transparent_out+fee in memory (v2_send.cpp:507) but it's DROPPED on serialize ⇒ any nonzero transparent payout ⇒ conservation or SMILE-balance-proof mismatch ⇒ `bad-shielded-v2-send-encoding`/reject. ⇒ **a post-fork self-service unshield with a real payout CANNOT be encoded.** Shipping it REQUIRES a CONSENSUS CHANGE / hard fork: a NEW unshield-capable encoding that serializes value_balance + relaxing RejectDirectSendEncodingForFork. (Reconcile w/ x2: x2 showed the consensus CHECKS hold for any valid tx; x3 shows you can't BUILD a valid unshield tx post-fork. x3's §4 empirical confirmation was incomplete — confirm by actually submitting one, but the code-chain is specific+strong.) ⇒ w1's "re-enable the gate" impl is INVALID; the feature is bigger/riskier than the "ship fully" decision assumed.
- PRIVACY (if a fork adds the encoding): amount de-anon is SEVERE/intrinsic — value_balance + transparent vout.nValue are PLAINTEXT (bundle.cpp:1050); an OTC-style unusual-amount unshield de-anonymizes the unshielder w/ prob≈1 by amount-matching. Fee bucketed to 1000-sat (hides fee low-bits, NOT the payout); change note hidden (good); no enforced broadcast delay ⇒ timing correlation with a preceding z→z. ⇒ the v1 "privacy win" is limited (operator-linkage removed, but released AMOUNT is public regardless).
- DoS: self-service unshield makes d3's O(chain) rebuild PERMISSIONLESS (attacker ring_positions → bad-shielded-ring-member-position → auto-rebuild from MemPoolAccept). HIGH — **but ALREADY CLOSED by i2's d3 fix** (rebuild removed from MemPoolAccept). Confirm i2 covers this exact path.
- MEMORY: x3's ASan was incomplete (pending export); SUPERSEDED by i3 (root-caused = stack overflow, fixed).

## ★ x2 — unshield ECONOMIC attacks all HOLD + KEY REFRAMING of the f3/feature risk (agent x2)
- Double-spend (same note via unshield: within-block, cross-block, reorg; two distinct nullifiers for one note), value-conservation/inflation (transparent_out+fee > shielded-in; oversized/negative value_balance; MAX_MONEY overflow; turnstile underflow), replay/payout-malleability, mempool acceptance of invalid unshield — **ALL REJECTED.** The consensus checks (nullifier set, turnstile pool≥0, CheckTxInputs value_balance fold, whole-tx statement-digest binding, SMILE2 balance) are FAMILY-AGNOSTIC ⇒ an unshield V2_SEND gets the EXACT SAME enforcement as a z→z send. Confirms+stress-extends c5/e3/d1 onto z→t (incl reorg/cross-block/mempool empirically).
- ★★ REFRAMING (important for the ship-fully + f3 decision): the wallet gate (shielded_wallet.cpp:2990 / shielded_privacy.cpp:61) is **WALLET-CLIENT POLICY, NOT a consensus/relay boundary.** Full SMILE2 CShieldedProofCheck runs at MEMPOOL admission (not just block-connect). So an attacker can hand-craft + broadcast an unshield raw tx TODAY, gate or no gate — **f3 is ALREADY permissionless at consensus level.** ⇒ enabling the wallet FEATURE does NOT materially expand the ATTACKER's f3 access (attackers don't use the wallet); it raises HONEST-USER volume. The marginal f3 risk from shipping the feature is therefore LOWER than v1/PR_PLAN framed — though f3 still underpins ALL shielded value and still needs the audit + x1's forge result.

## v1 — self-service unshield = NON-CONSENSUS wallet feature, BUT hard-coupled to f3 + segfault (agent v1)
- VERDICT: NO fork. The path (direct V2_SEND with transparent outputs: value_balance = transparent_out+fee, carries a real SMILE2 spend proof) is ALREADY built + consensus-valid (builder v2_send.cpp:479, z_sendmany routing shielded_rpc.cpp:8570, CreateV2Send shielded_wallet.cpp:3425). NO consensus rule rejects it (RejectDirectSendEncodingForFork is encoding-only). Disabled purely by a WALLET-POLICY early-return (shielded_wallet.cpp:2990, keyed to nShieldedMatRiCTDisableHeight — a privacy-redesign nudge, NOT a security gate). ⇒ feature = re-enable/relax that policy + UX. ~1-2 days of code. (The "bridge unshield"/V2_EGRESS_BATCH is a separate L2/operator netting model — operator-attested statement_hex, NO shielded proof, the linkage-leak path; self-service avoids it = privacy WIN.)
- Value-conservation on z→t reuses c5's present checks (h2 balance + CheckTxInputs fold + turnstile pool≥0) — confirmed. Privacy: clear win (no operator metadata; standard amount/timing/change residuals already mitigated by min-anonset/ring/dust/fee-bucket/output-shuffle).
- ★★ TWO HARD PREREQUISITES (the feature is trivial; these dominate + RAISE the stakes):
  1. i3 SMILE2 prover SIGSEGV (4th confirmation by v1; intermittent/heap-dependent ⇒ uninitialized/OOB read) MUST be fixed — unshield uses that exact prover (CreateSmileProof→TryProveCT).
  2. **f3/C-002 MUST be resolved (i5 swap or audit) BEFORE shipping self-service unshield** — the unshield proof IS a SMILE2 CT proof, so self-service turns the unproven 2^-8-monomial soundness into a PERMISSIONLESS, high-volume, operator-free VALUE-EXTRACTION (inflation) surface. ⇒ f3 fix is NO LONGER optional if the feature ships. Feature + f3-fix reinforce each other in 0.30.3.

## h2 — SOFT-FORK BUNDLE implemented + verified; 2 self-inflicted bugs caught (agent h2)
- a5 halt CLOSED (reconciled cap max(prev_mtp+drift, prev_blocktime−MAX_TIMEWARP); reproduced inversion → fixed, block mined, chain continued; consensus+miner+submitSolution identical; honest mining unaffected).
- Drift→1800 works (honest max block.time−prev_mtp=540s ≪ 1800, NOT clipped; per-block lever ×1.97@3600 vs ×1.39@1800; catch-up/over-stall blocks accepted).
- Height-gate mining clamp + enable-on-all-nets works (b6-F1/c1 flag mismatch fixed; CLI-enableable regtest). Interaction: ALL hardening together = no inverted/empty window, no stuck chain, no honest-block rejection, liveness preserved.
- ★ IMPLEMENTATION GOTCHAS for i-agents/integration: (1) **slew guard must be WIRED into MatMulAsert (pow.cpp)** — `ApplyDgwSlewGuard` is DGW-only/DEAD for MatMul, so just enabling the height does NOTHING; once wired it caps per-block Δdiff to ×4 (×51.5→×4.0 demoed), honest sub-×4 unchanged.
- ★★ **CUMULATIVE DRIFT CAP = RECOMMEND DROP.** h2 found the naive version has TWO fail-closed chain-halt bugs (rejected EVERY block from activation — WORSE than a5). Even done correctly (must measure excess inter-block GAP not distance-above-MTP, AND gate look-back at activation height, not across genesis) it is BELT-AND-SUSPENDERS ONLY: the per-block drift cap + 2h MAX_FUTURE_BLOCK_TIME bound (a2) + slow MTP advance already self-limit forward drift unless cumcap/window < per-block-drift. ⇒ a fragile, chain-halt-prone consensus rule that adds ~no real protection. RECOMMEND: DROP it from 0.30.3 (adding it makes the chain LESS safe, contradicting the goal). Keep ONLY if maintainer insists, implemented EXACTLY per h2's spec + heavy testing.

## CHAIN-HALT STATUS (answer to "permanent halt now?") — c1 RESULTS IN
- a5 inversion is ACTIVATION-BOUNDARY-ONLY ⇒ unreachable post-activation (capped blocks ≤ prev_mtp+3600 < +4200; MTP non-decreasing verified across 870+ blocks). Mainnet passed 118482 cleanly ⇒ NO current permanent-halt risk on mainnet from a5.
- **c1 CONFIRMS a5 is the ONLY timestamp halt** (enumerated the full 4-constraint timestamp rule set; only inversion = BIP94-floor ⊥ drift-cap). NEW detail: UpdateTime does NOT save you — in the inversion it emits min_time which itself violates the cap ⇒ honest miner SELF-REJECTS. ⇒ a5 fix must apply identically in consensus AND the miner clamp.
- **No MatMul poison/crashing block** (c1): all throw/assert/OOB paths dominated by Phase1 dim-pin + payload gates before matrix work; submitblock rejects bombs cleanly. (UB code-reasoned — no sanitizer build on host; c4 doing sanitizer build in docker for the SHIELDED path.)
- **Difficulty lock-out = MED, SELF-HEALING (c1):** min-gap spike ×7.4 over ~760 blocks (HL=14400); recovers in ~50 honest blocks, chain stays LIVE (not a permanent freeze). NOTE: per-block slew guard `ApplyDgwSlewGuard` is WIRED BUT DISABLED on all networks — enabling it would cap spikes (optional consensus hardening, evaluate). Sustained majority attacker extends the transient but pays self-funded PoW.
- **No validation/relay deadlock / permanent wedge** found.
- ★ OPEN: SHIELDED poison surface NOT exhausted — c1 flagged `std::get<SendPayload>` bad_variant_access (bundle.cpp:510) + NDEBUG-stripped asserts in ct_proof/membership ⇒ agent c4 hunting (potential CRIT remote-crash if reachable).

## b7 — DEFINITIVE difficulty resolution (a1 vs a2 settled) + drift value (agent b7)
- Measured under realistic advancing-clock + live ASERT + mainnet HL=3600 (warmed an elevated baseline first — the a1/a2 gap was the regtest chain starting AT powLimit). RESULT: **BOUNDED ~×3.36 transient, NOT floor** (13th ceiling-stamped block hard-rejected "time-too-new" by the 2h bound). a1's "collapse to floor" = free-mocktime artifact (CONFIRMED). Self-correcting (fast mining recovers ≤60 blocks; genesis-anchor makes it sticky under 90s spacing). Severity MEDIUM. CONFIRMS a2.
- Per-block lever = 2^((drift−90)/HL): drift 3600/1800/900 → ×1.87/×1.32/×1.17; max sustained ×3.36/×2.73/×1.80. Honest miners SELF-CLAMP ⇒ blocks NEVER rejected at any drift. **drift=1800 never down-stamps honest (mainnet max 1143s < 1800)**; drift=900 clips ~0.7% tail.
- RANKED FIXES: (a) **drift 3600→1800 = PRIMARY** (zero honest impact, halves the lever; needs a5 inversion fix as prerequisite — h2 confirmed both). (b) **STACK: clamp the timestamp ASERT CONSUMES to min(blocktime, prev_mtp+900)** — ZERO honest/liveness impact, further floors the lever (~×1.8 residual, since prev_mtp is itself attacker-inflatable +600/block). (c) cumulative cap: anchor-relative form DEFEATED by prior fast-mining time-debt; only sliding-window robust — reinforces DROP (h2).

## a1 — ASERT difficulty grind: BOUNDED, not floor (a1 vs a2 RECONCILED)
- a1 "collapse to floor in 100 blk" used setmocktime advancing the validator clock each block ⇒ 2h MAX_FUTURE_BLOCK_TIME check never fired ⇒ unbounded fake-elapsed injection (HARNESS ARTIFACT). a2 (real clock) ⇒ ratchet pinned at +2h. REALISTIC (honest real clocks): max injectable fake elapsed ≈ 2h buffer ⇒ max difficulty drop ≈ 2^(7200/HL): mainnet HL=3600 ⇒ ~×4 transient, self-correcting (NOT floor). Per-block lever still ~×2 (drift==HL). Liveness under stalls = SAFE (miner clamps into past, self-heals) — CONFIRMED. SEVERITY realistic = MED. → b7 measures difficulty under realistic-clock+live-ASERT+mainnet-HL definitively and tests fixes.

## ★ HIGHEST-SEVERITY FINDING — activation-boundary chain-halt (agent a5)
- **HIGH (latent, dormant on mainnet): timewarp lower bound ⊥ drift upper bound inversion.** Lower=max(prev_mtp+1, prev_blocktime−600[MAX_TIMEWARP, BIP94 always-on mainnet]); upper=prev_mtp+3600. If prev_blocktime>prev_mtp+4200 the legal window is EMPTY ⇒ next block unmineable by anyone ⇒ PERMANENT chain halt. Can ONLY occur at the activation boundary (block 118481 isn't capped, can be stamped up to +7200). Mainnet PASSED it cleanly (UpdateTime's ungated clamp held honest 118481 ≤ mtp+3600); POST-activation it's impossible (capped blocks ≤ prev_mtp+3600 < +4200). DORMANT on mainnet now, but REINTRODUCED on any network where the cap activates at height>0 (see b4-1) unless fixed.
- **FIX (must-have in PR):** reconcile the rules so a block is never unmineable — consensus upper bound = max(prev_mtp+drift, prev_blocktime − MAX_TIMEWARP); miner clamp must match. (And/or activate from genesis on new networks.)

## b5 — reorg/selfish economics: cap NEUTRAL-to-HELPFUL; +1 wallet bug (agent b5)
- ASERT = absolute-anchor aserti3-2d, memoryless (corroborates b4). A7 difficulty-raising self-limiting (needs own hashrate); cap NEUTRAL.
- D3 selfish mining: ~1/3 Eyal-Sirer threshold; ASERT fast retarget does NOT raise it (marginally helps attacker). Cap NEUTRAL. Consensus "fix" would be worse than disease (unenforceable) ⇒ DOCUMENT, don't code (matches external #4 triage).
- D4 deep-reorg/time-bandit: generic >50% double-spend confirmed (cap-OFF). "ASERT drop ⇒ cheap reorg" REFUTED (slow fork also slashes chainwork; reorg stays a chainwork race, ASERT-neutral). **PR#214 cap HELPS D4**: throttles single-block diff-drop lever ×3.93+/unbounded → ×1.97; liveness preserved (honest 3000s-gap late blocks accepted; over-cap clamped at mining).
- **NEW (low-med): reorg wallet-confirmation desync.** After a reorg that EXCLUDES (vs conflicting-spends) a payment, honest wallet keeps stale `confirmations` (saw conf=3) with empty `walletconflicts` though the UTXO is gone ⇒ misleads merchants reading naive conf counts. FIX: detect "not in active chain and not in mempool" → mark conflicted; advise UTXO checks. (Wallet correctness, not consensus.)

## b6 — corroborates a5; +1 small fix (agent b6)
- a5 chain-halt RECONCILED: requires an attacker/clock-skewed miner to INJECT a high-timestamp predecessor at the unprotected pre-activation block (a5 did this via submitblock); the honest miner's always-on clamp self-protects (b6 saw NO inversion over 30 honestly-clocked blocks). ⇒ HIGH impact, attacker-triggered, boundary-only, dormant on mainnet. Fix still warranted.
- **F1 (NEW, low): height-gating mismatch.** GetMaximumTime/MatMulFutureBlockTimeLimit (mining + submitSolution) is NOT height-gated while consensus MaxMatMulFutureBlockTime IS. At height 199 (below activation 200): consensus_active=false but mining clamps curtime to prev_mtp+3600 (while clock allows +7200); submitSolution HARD-REJECTS consensus-valid timestamps below activation (submitblock unaffected). Conservative/bounded. FIX (add to list): height-gate the mining/submitSolution path to match consensus.
- Re-confirmed robust: INT64 overflow guard, RPC/CHECK_NONFATAL (no crash), hex-size/submitheader guards, pre-hash gate relocation (cheap SHA256, still enforced full-block; no validity change), cap boundary inclusive-safe, BIP113 lower bound never violated.

## a2 — MTP ratchet is BOUNDED; timelock attack REFUTED (agent a2)
- Per-block MTP advance = exactly 600 s/block (median dampens 3600/6). Absolute lead capped at +7200s (2h) by MAX_FUTURE_BLOCK_TIME (time-too-new). Timelock (BIP113) maturation bounded to ~2h early = pre-existing Bitcoin tolerance, NOT widened ⇒ external vectors #1 (runaway ratchet) & #4 (timelock) REFUTED. The cumulative-window cap is therefore LOWER priority than first thought — the 2h bound already caps cumulative lead.

## ASERT internals — math ROBUST, but 2 actionable items (agent b4)
- ASERT is stateless/path-independent (target = pure fn of fixed anchor + tip time/height; aserti3-2d, __int128, 256-bit saturating). NO rounding-bias ratchet (mean-preserving oscillation gave exactly periodic drift-free target), NO overflow/assert on extreme inputs (clamp to powLimit / target=1), anchor unbiasable without reorg below activation (mainnet 50000), boundary re-anchors exact (no off-by-one).
- **FINDING b4-1 (med, ACTIONABLE): the PR future-drift cap is MAINNET-ONLY** — nMatMulMaxFutureMtpDriftHeight set only for mainnet (chainparams.cpp:202); testnet/signet/regtest leave it INT32_MAX. Confirmed: off-mainnet a +100000s future block was ACCEPTED and collapsed next block to powLimit in one step (only MAX_FUTURE_BLOCK_TIME=7200 bounds it). FIX: enable the cap on ALL MatMul networks.
- **FINDING b4-2 (med): residual headroom** — (a) cap == exactly one half-life ⇒ single max-future-drift block still ~halves difficulty (×1.97 ease/block; ×4 transient, bounded by the 2h future limit). (b) **past-dating is uncapped by the PR** but BIP94 MAX_TIMEWARP floors it to ~600s below prevMTP; a block 538s before its parent RAISED next difficulty (the difficulty-RAISING lever, weaker than future since 600s«3600s).
- A7 difficulty-raising: minimal-gap blocks harden ~×1.017/block (×2 over ~40 blocks @ mainnet HL=3600) but attacker pays full PoW at the rising difficulty ⇒ self-funded; only profitable combined with selfish mining (→ b5).

## Relay/DoS hardening — all guards HOLD (agent a3)
- C1 BLOCK_MUTATED griefing: NO remote amplification (P2P → one-shot 100 ban; loop only via local authenticated submitblock RPC). It's a DoS-resistance IMPROVEMENT (bad uncommitted payload can't permanently invalidate the real block).
- C3 header pre-hash 'high-hash' gate: STRICT RELAXATION of the work check (target<<eps saturates) ⇒ CANNOT orphan honest blocks; DeriveSigma O(1) before matmul ⇒ no work amplification.
- C4 submitblock guards (hex parity / 32MB HTTP cap / compactsize-bomb / decode) all hold, memory flat, no crash/bypass.
- **C2 product-digest tightening = CONFIRMED but LOW risk.** Post-activation, a transcript-digest block ALSO carrying a C' payload is rejected by PR-214 / accepted by main (digest-active mainnet ≥61000). Reference miner only emits product-committed-digest blocks (never collides) ⇒ only non-conformant/legacy miners affected. ACTION: confirm no legacy miners emit transcript-digest+payload at heights ≥61000.

## Confirmed non-consensus findings (agent a7)
- **C5 chain_guard griefing = CONFIRMED (med).** Eclipse/stall a victim's OUTBOUND peers (guard counts outbound only, default min 2) → should_pause_mining → PR's getblocktemplate/getmatmulchallenge THROW -9 → victim mining halts (cheap, sustained). NOT a chain split (node keeps validating/relaying). Guard ON by default mainnet, OFF on test chains.
- **getblocktemplate-throws-on-pause = PR REGRESSION (low-med).** main returns a usable template + chain_guard warning; pr-214 throws RPC -9. => ACTIONABLE PR FIX: revert ONLY the GBT throw to bypass+return-template-with-warning; keep submitblock/submitSolution refusing. (preserves BIP22/23 poller compat.)
- **C6 NTP/clock skew = real but PRE-EXISTING/upstream (operational).** >2h backward → node rejects honest blocks & desyncs; >2h forward → self-orphan. PR's MTP rule is MTP-keyed (not local clock) so skew does NOT trip "time-mtp-too-new". Fix = NTP hygiene, not code.
- submitblock hex guard / submitheader-reject / submitSolution stale checks: benign-positive.

## Triage of the 10 externally-proposed vectors (orchestrator)
- MTP-ratchet/cumulative/boundary (1,3,5,7): REAL residual but bounded by MAX_FUTURE_BLOCK_TIME (≤2h lead).
  Per-block lever is one half-life. Mitigation = cumulative windowed future-drift cap. (testing a1/a2)
- Backward/oscillation (2,6): proposer's sign analysis looks INVERTED; verify empirically (a6) before adding any past-bound
  (over-tightening rejects honest late blocks ⇒ liveness risk).
- Selfish mining (4): pre-existing, general, not timestamp-fixable. Out of scope (document).
- Cross-version trap (8): = soft-fork split risk; operational (upgrade coordination), not code. (a4)
- chain_guard/eclipse (9), NTP (10): non-consensus/generic; assess PR's new getblocktemplate-throw regression (a7).

## Mitigation design — REVISED with evidence (running PR fix-list, priority order)
1. **[HIGH, must] Fix activation-boundary chain-halt (a5).** Consensus upper bound = max(prev_mtp+drift, prev_blocktime−MAX_TIMEWARP); make miner clamp (UpdateTime/GetMaximumTime) agree so it can never emit/accept an unmineable window. Prevents the timewarp⊥drift deadlock on any (re)activation.
2. **[MED, should] Tighten per-block drift below one half-life.** drift==half-life(3600) ⇒ one max block ~×2 difficulty (b4). Live honest block.time−prev_mtp ≤ ~1143s ⇒ drift≈1800 is safe and caps single-block ease to ~×1.41. Confirm honest distribution with a1/a6 before finalizing. (Interacts with #1: smaller drift widens the inversion gap, so #1 is prerequisite.)
3. **[MED, should] Enable the drift cap on ALL MatMul networks (b4-1)**, not just mainnet (off-mainnet a future block collapses difficulty to powLimit). Activate from genesis on test networks (also sidesteps #1 there) ⇒ makes the rule natively testable.
4. **[LOW-MED] Revert getblocktemplate-throw-on-guard to bypass+return-template-with-warning (a7)**; keep submitblock/submitSolution refusing. Fixes BIP22/23 poller regression.
4b. **[LOW] Height-gate the mining/submitSolution future-time clamp (b6-F1)** to match the consensus activation height, so below activation the miner/submitSolution don't apply a stricter-than-consensus bound. (Or accept as benign conservative behavior.)
5. **[LOW, optional] Cumulative windowed future-drift cap** — DEMOTED: a2 shows the 2h clock bound already caps cumulative lead; keep only as defense-in-depth if cheap.
6. **[LOW, optional] Freivalds rounds 2→3 (b1); deserialization length bound on matmul payload (b2).** Defense-in-depth; not required.
8. **[HIGH crypto-quality, must-fix — likely SEPARATE commit/PR] fail-closed RNG (c3).** src/libbitcoinpqc/src/ml_dsa/utils.c: abort on /dev/urandom open-fail and short read (no zero-fill); error (don't wrap/reuse) on insufficient injected entropy. Verify production keygen path. (Crypto fix — consider isolating from the consensus PR so it can be reviewed/merged independently.)
10. **[LOW, defense-in-depth] shielded refactor-proofing (c4):** locally guard variant std::get sites (get_if/holds_alternative); convert redundant size/bounds asserts in ct_proof/membership to real runtime checks (so release-build safety doesn't depend on derived-tag invariant / NDEBUG).
11. **[LOW, optional consensus] enable per-block difficulty slew guard `ApplyDgwSlewGuard` (c1)** — wired but disabled on all networks; would cap difficulty-spike lock-out transients. Evaluate carefully (consensus change).
9. **[MED, docs/ops] supply-chain hygiene (c3):** bump depends/ sqlite ≥3.50.2; pin Dockerfile apk versions; checksum CI curl|tar fetches; record kyber/sphincs upstream commit+hash; fix libbitcoinpqc provenance URL; commission external audit of bespoke matmul/shielded crypto.
7. **Backward-drift: DECIDED — keep cap FUTURE-ONLY, add NO past bound (a6).** a6 measured the sign (past-dating ⇒ difficulty UP, hurts attacker; future ⇒ DOWN). Oscillation NOT profitable: BIP94 (MAX_TIMEWARP=600) blocks the past leg; the only difficulty-lowering move (future) is already cap+2h bounded ⇒ ≤1 blk/hr vs 40 honest, no net bias. A past bound adds no security + liveness risk (rejects honest late blocks). External #2/#6 REFUTED.
- Out-of-scope (document, not consensus code): selfish mining (#4-ext), cross-version split (#8, operational), chain-guard eclipse (#9, ops + #4 above), NTP (#10, ops).
- ACTION: confirm no legacy miners emit transcript-digest+payload at mainnet ≥61000 (a3) before relying on the product-digest tightening.

## ⚠ METHODOLOGY CAVEAT — shared ruleon image is BROKEN
- `btx-node:pr214-regtest-ruleon` reports future_mtp_limit_height = 2147483647 (rule DISABLED). The orchestrator's patch hit the WRONG test-chain section (two chains share the `nMatMulProductDigestHeight=0 / nMatMulPreHashEpsilonBits=0` anchor; the FIRST occurrence ≠ CRegTestParams). Agents a2/a4/a5/b4 detected this and built their OWN correctly-activated images (valid). ANY agent that relied on the shared image for "rule active" empirics produced INVALID rule-behavior results — scrutinize a1/a6/b5/b6 for this; trust only self-built/CLI-arg-activated runs.
- CORRECT activation in regtest: either patch CRegTestParams specifically, or use a4's CLI args `-regtestmatmulmaxfuturemtpdriftheight=200 -regtestmatmulmaxfuturemtpdrift=3600` (4 idiomatic edits; FOLD INTO THE PR — makes the rule natively testable, supports b4-1 "enable on all networks").

## a4 — soft-fork split model holds EXACTLY (agent a4)
- Minority attacker ⇒ violating block orphaned, never relayed (header-stage reject). Non-enforcing majority ⇒ enforcers stranded on lower-work chain, never reorg onto invalid chain, HEAL when honest work leads (converged at 212 after 7 compliant blocks). Boundary off-by-one-correct: active exactly at 200, strict `>` (3600 ok, 3601 reject). Mining-policy/consensus flag divergence below activation = cosmetic. Stranding = known operational deploy caveat (majority enforcement), not a code bug.

## Wave D — aggressive/forgotten/regression hunt (SPAWNED)
- d1 historical-CVE regression sweep (CVE-2018-17144 dup-input inflation, 2010-5139 value overflow, 2012-2459 merkle malleability, BIP30/34/66, subsidy) + FORK-BASE staleness (missing post-fork Core security patches).
- d2 ★ MatMul BACKEND non-determinism (CPU/Metal/CUDA/MLX + float/SIMD/reassociation) → targeted consensus split. HIGH novelty.
- d3 verification-cost AMPLIFICATION + index/mempool/disk exhaustion (multi-block dimension; b2/c1/c2 cleared single-block).
- d4 miner-economic + BTX-NOVEL service-challenge subsystem (getmatmulservicechallenge/redeemmatmulserviceproof double-redeem/replay/forge) + orphan/chain_guard griefing.

## Wave E/F/H/S — deeper crypto breaks, fix-regression, supply-chain (SPAWNED)
- e5 ADVERSARIALLY VERIFYING the e1 CRIT (reproduce break + calibrate severity + validate nonce-in-seed fix + confirm hard-fork).
- f1 deeper matmul amortization (is the e1 fix complete? other amortizable/precomputable per-nonce work; sigma-lottery shortcut).
- f2 matmul field/algebraic structure (Mersenne NTT, structured-matrix sub-cubic, Strassen, GPU/ASIC asymmetry).
- f3 shielding lattice cryptanalysis #2 (identify MatRiCT/SMILE paper; online-research known attacks/errata; param security → inflation).
- h1 PoW nonce-in-seed FIX regression analysis (defeats e1? perf/consensus/cache/service-challenge regressions; builds+tests).
- h2 soft-fork 125k BUNDLE regression + re-prove a5/a1/a2/c1 closed (interaction/liveness; drift=1800 honest-clipping; slew-guard).
- s1 supply-chain standard libs (boost/libevent/zeromq/sqlite/leveldb/zlib/crc32c: version/CVE/reachability/pinning).
- s2 supply-chain vendored bitcoin libs (secp256k1/minisketch/univalue: upstream commit + MODIFIED? + staleness — backdoor risk).
- s3 supply-chain PQ deps provenance (dilithium/sphincs/kyber pins) + build/CI/base-image/depends integrity (unpinned apk, curl|tar no-checksum, guix).

## Wave status
- Wave 1 (a1..a7): RUNNING. Wave 2 (b1..b5): SPAWNED — Freivalds soundness, verification DoS/OOB fields,
  pre-hash/PoW grinding, ASERT internals/boundaries, reorg/selfish economics.

## Refuted / robust (don't repeat)
- **C5 shielded MONETARY soundness (impl level) — REFUTED (robust), agent c5.** Value conservation (tx_verify.cpp:203, smile2 VerifyCT balance, pool≥0), range proofs (MatRiCT VALUE_BITS, smile2 base-4), double-spend/nullifier (within+cross-block, unconditional in ConnectBlock, not relaxed by assumevalid), membership/anchor (tracked historical root, fail-closed), bridge mint/burn (IngressBatchPayload forces consumed_spends≥1), proof binding (statement digest hashes whole stripped tx + fork) — ALL present & by-construction-correct. Residual = soundness of the bespoke lattice PRIMITIVES → e2/e3/e4 + external audit. NEW: **MED z_sendmany(z→z) prover SIGSEGV** (smile2, local/sender-side, no inflation, shielded sends broken, blocks tamper tests → ASan); **LOW NeedsShieldedProofCheck** infers from input-count not family (make explicit).
- **D2 MatMul BACKEND non-determinism → split — REFUTED (robust by construction), agent d2.** Consensus = single pinned pure-INTEGER CPU path; CPU/Metal/CUDA/MLX dispatch is MINING-ONLY; winning GPU candidate recomputed on CPU & block carries CPU digest ⇒ buggy backend only self-orphans the miner, never splits. No float in consensus (field mod 2^31-1, ASERT __int128); no -ffast-math/-march=native. SIMD/thread determinism proven (2M trials + bit-identical across -O0/-O3/-march=native/-Ofast/-ffast-math; multithreaded==naive). LOW: add build-independent NeonDot==ScalarDot unit test. Lead: ARM not run on real Apple Silicon.
- **D1 historical-CVE regression + fork-base staleness — REFUTED (robust), agent d1.** CVE-2018-17144 dup-input inflation (bad-txns-inputs-duplicate, tx_check.cpp:66), 2010-5139 value overflow (bad-txns-txouttotal-toolarge), 2012-2459 merkle dup-txid (IsBlockMutated), BIP30/34/66 + subsidy/halving + coinbase maturity — ALL enforced (empirically on mempool+block path). Shielded↔transparent boundary range-checked (MoneyRangeSigned + CheckedAdd). FORK BASE = Bitcoin Knots 29.2.knots20251110 (~Nov 2025); Oct-2025 Core v30 sec fixes backported (CVE-2025-54605/54604 log-disk, 2024-52911 script UAF, INVDoS via weight-orphanage). Staleness minimal. Low lead: CVE-2025-46598 (v30 mempool resource) — verify backport.
- **C4 shielded/bridge POISON-BLOCK surface — REFUTED (robust by construction), agent c4.** (1) std::get<SendPayload> bad_variant_access (c1's lead, bundle.cpp:510): SAFE — FamilyPayload tag is DERIVED from the active alternative (GetPayloadFamily = holds_alternative chain, v2_bundle.cpp:1144), so switch→std::get is tautologically correct; wire family_id only picks which alternative to CONSTRUCT (invalid→clean ios_base::failure), never a std::get key; variant can't go valueless. (2) NDEBUG-stripped asserts (ct_proof/membership): SAFE — each is redundant with a real throw/if-return; bounds enforced by explicit `if(size!=..)return false` present in release. (3) codecs: constant divisors, NTT overflow-guarded, sizes pinned before index, loops min-clamped. (4) bridge DecryptViewGrant: size/underflow-guarded, behind operator secret key (wallet, not validation). Two LOW defense-in-depth fixes for PR: locally guard the std::get sites (get_if/holds_alternative); convert size/bounds asserts to real runtime checks. (code-reasoned by-construction, no ASan run.)
- **C2 P2P / net_processing (takedown + RCE) — REFUTED (robust), agent c2.** No crit/high. Empirically fuzzed (raw-socket P2P client, full NODE_SHIELDED handshake): shielded-v2 TransactionBundle deserialize (the prime buffer-overflow candidate) — every count bounded-compactsize-before-alloc or explicit >MAX throw; proof-shard offset+size ≤ payload.size() uint64-checked ⇒ malformed → clean ios_base::failure → peer ban, RSS flat (~157→163MB) incl 60× reconnect flood. MatMul payload: dim pinned & size-checked before indexing (re-confirms b2). getshlddata/shieldeddata: rate-limited, capped, unsolicited rejected pre-parse. PQ MLDSA/Dilithium verify: double size-gated before buffer ops (no attacker-length memcpy). Only low/defense-in-depth notes (dead-path reserve(≤4096); b2's pre-decode transient). LEAD: empirical MatMul-payload OOB needs a header-PoW-valid-block + mutated-trailing-payload harness (checks sit behind header PoW) — c1 may cover.
- **B2 / B3-DoS / E2 verification-asymmetry + OOB fields — REFUTED (robust).** matmul_dim must == nMatMulDimension EXACTLY (CheckMatMulProofOfWork_Phase1 in CheckBlockHeader) BEFORE any matrix alloc / O(n^2)/O(n^3) work → every pathological dim (0,1,non-multiple,2048,65535) = cheap "high-hash" reject. Worst-case valid-shape rejection costs verifier ~1.3ms (max 3.8ms) vs attacker ~91ms/block ⇒ DoS ratio <1 (defender-favorable). Freivalds C' payload shape/element-range checked cheaply; oversized = clean decode-fail (chunked alloc, MAX_SIZE cap). seeds enforced (bad-matmul-seeds). dual-nonce non-issue (only nNonce64 serialized). nBits edges rejected (DeriveTarget). 24MB weight boundary counts the payload; 16MB transport limit is stricter. asserts not adversary-reachable (n pinned). (agent b2)
  - Minor hardening nicety (NOT a vuln, optional for PR): junk matrix_c_data is hex-decoded/allocated up to ~16MB transport cap BEFORE the cheap header dim check; a deserialization-side length bound to nMatMulMaxDimension^2 words removes that transient.
- **B1 Freivalds PoW forgery — REFUTED (crown jewel is safe).** Challenge r = f(SHA256(0xF4 ‖ sigma ‖ H(A') ‖ H(B') ‖ H(C'))) — r depends on C' itself (Fiat-Shamir), so you cannot grind r then place C'−A'B' in its kernel; any C' change re-randomizes r. Soundness ≤ 2^-62 (k=2 rounds, p=2^31−1). C' is also header-committed via matmul_digest (ComputeProductCommittedDigest) when product-digest active (mainnet h≥61000); below that, payload blocks must pass full O(n^3) Phase2. Empirical: 0 forgeries / >800k grind attempts in a faithful reimpl. Low-rank/sub-cubic shortcut (B4 part) also refuted: AB term is irreducibly dense O(n^3); noise terms rank≤r are O(n^2). Source: src/matmul freivalds.cpp DeriveChallengeSeed. (agent b1)
  - Residual hardening (LOW priority, defense-in-depth for the PR): bump nMatMulFreivaldsRounds 2→3 (2^-62 → 2^-93) — one extra O(n^2) pass. NOTE: sigma deliberately EXCLUDES matmul_digest to avoid circularity — correct, do NOT change.
  - Handoff: large in-range matmul_dim with an n^2 payload still imposes noise-gen + Freivalds O(n^2) verify cost BEFORE rejection — a verification-asymmetry/DoS question for b2, not a soundness break.
