# 0.34.1 golden freeze

This commit is the freeze `F` for v0.34.1. It does not change BUILD_RELEVANT
paths (`CMakeLists.txt`, `cmake/`, `src/`, `contrib/matmul-v4/`). The proven
tree is parent `923c081ece2a948ada94e89bb5fdfbeafdf8f9d9`.

Both corpus hosts linked `btxd`, `btx-cli`, and `matmul-v4-rc-harness` with
`-DWITH_ZMQ=ON` before this freeze:

| Backend | Host | Configure | Verify |
|---|---|---|---|
| CUDA `sm_120` | macpro2, tree `~/btx-0341-freeze` | `PKG_CONFIG_PATH` + `CMAKE_PREFIX_PATH` at `/home/administrator/opt/zmq`; `-DCMAKE_CUDA_COMPILER=/usr/local/cuda/bin/nvcc`; `-DCMAKE_CUDA_ARCHITECTURES=120` | `ldd` → `libzmq.so.5`; `verify_release_btxd.py` PASS; `btxd -version` = v0.34.1 |
| Metal | macstudio-1, tree `~/Documents/btx-0341-metal` | Homebrew prefix; `-DBTX_ENABLE_METAL=ON` | `otool -L` has no Homebrew/zmq dylibs; `verify_release_btxd.py` PASS; `btxd -version` = v0.34.1 |

Corpus measurement follows this commit. Any later BUILD_RELEVANT change burns
the recorded episodes.
