# 0.34.2 golden freeze

This commit is the freeze `F` for v0.34.2. It bumps
`CLIENT_VERSION_BUILD` to 2. Parent is
`1a58e07a92029afd9b9f0ea440110bfe0530fc8b` (withdraw EncDr stall-recovery
re-anchor on mainnet). Both are BUILD_RELEVANT relative to the 0.34.1
seal; the golden manifest must be remeasured.

Corpus hosts link `btxd`, `btx-cli`, and `matmul-v4-rc-harness` from this
commit with `-DWITH_ZMQ=ON`:

| Backend | Host | Configure | Verify |
|---|---|---|---|
| CUDA `sm_120` | macpro2, tree `~/btx-0341-freeze` | `PKG_CONFIG_PATH` + `CMAKE_PREFIX_PATH` at `/home/administrator/opt/zmq`; `-DCMAKE_CUDA_COMPILER=/usr/local/cuda/bin/nvcc`; `-DCMAKE_CUDA_ARCHITECTURES=120` | `ldd` → `libzmq.so.5`; `verify_release_btxd.py` PASS; `btxd -version` = v0.34.2 |
| Metal | macstudio-1, tree `~/Documents/btx-0341-metal` | Homebrew prefix; `-DBTX_ENABLE_METAL=ON` | `otool -L` has no Homebrew/zmq dylibs; `verify_release_btxd.py` PASS; `btxd -version` = v0.34.2 |
| CPU | cymacpro-linux, tree `build-cpu-0341` | GCC 13; `-DWITH_ZMQ=ON` | `ldd` → `libzmq.so.5`; `verify_release_btxd.py` PASS; `btxd -version` = v0.34.2 |

Harnesses must be linked at this freeze so `embedded_source_revision`
matches `F`. Any later BUILD_RELEVANT change burns the recorded episodes.
