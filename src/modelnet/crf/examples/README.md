# Synthetic conformance examples

The 18 positive envelopes validate structure and canonical body IDs. Their all-zero signature bytes are deliberate non-cryptographic placeholders and MUST fail real signature verification. They are not approved policies, keys, funds, prices or customer evidence. Eight negative fixtures fail schema validation. Semantic cross-field rules and cryptographic authority require native tests.
